// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/davidroman0O/tempolite/ent/featureflagversion"
	"github.com/davidroman0O/tempolite/ent/predicate"
)

// FeatureFlagVersionQuery is the builder for querying FeatureFlagVersion entities.
type FeatureFlagVersionQuery struct {
	config
	ctx        *QueryContext
	order      []featureflagversion.OrderOption
	inters     []Interceptor
	predicates []predicate.FeatureFlagVersion
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the FeatureFlagVersionQuery builder.
func (ffvq *FeatureFlagVersionQuery) Where(ps ...predicate.FeatureFlagVersion) *FeatureFlagVersionQuery {
	ffvq.predicates = append(ffvq.predicates, ps...)
	return ffvq
}

// Limit the number of records to be returned by this query.
func (ffvq *FeatureFlagVersionQuery) Limit(limit int) *FeatureFlagVersionQuery {
	ffvq.ctx.Limit = &limit
	return ffvq
}

// Offset to start from.
func (ffvq *FeatureFlagVersionQuery) Offset(offset int) *FeatureFlagVersionQuery {
	ffvq.ctx.Offset = &offset
	return ffvq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ffvq *FeatureFlagVersionQuery) Unique(unique bool) *FeatureFlagVersionQuery {
	ffvq.ctx.Unique = &unique
	return ffvq
}

// Order specifies how the records should be ordered.
func (ffvq *FeatureFlagVersionQuery) Order(o ...featureflagversion.OrderOption) *FeatureFlagVersionQuery {
	ffvq.order = append(ffvq.order, o...)
	return ffvq
}

// First returns the first FeatureFlagVersion entity from the query.
// Returns a *NotFoundError when no FeatureFlagVersion was found.
func (ffvq *FeatureFlagVersionQuery) First(ctx context.Context) (*FeatureFlagVersion, error) {
	nodes, err := ffvq.Limit(1).All(setContextOp(ctx, ffvq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{featureflagversion.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ffvq *FeatureFlagVersionQuery) FirstX(ctx context.Context) *FeatureFlagVersion {
	node, err := ffvq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first FeatureFlagVersion ID from the query.
// Returns a *NotFoundError when no FeatureFlagVersion ID was found.
func (ffvq *FeatureFlagVersionQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ffvq.Limit(1).IDs(setContextOp(ctx, ffvq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{featureflagversion.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ffvq *FeatureFlagVersionQuery) FirstIDX(ctx context.Context) int {
	id, err := ffvq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single FeatureFlagVersion entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one FeatureFlagVersion entity is found.
// Returns a *NotFoundError when no FeatureFlagVersion entities are found.
func (ffvq *FeatureFlagVersionQuery) Only(ctx context.Context) (*FeatureFlagVersion, error) {
	nodes, err := ffvq.Limit(2).All(setContextOp(ctx, ffvq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{featureflagversion.Label}
	default:
		return nil, &NotSingularError{featureflagversion.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ffvq *FeatureFlagVersionQuery) OnlyX(ctx context.Context) *FeatureFlagVersion {
	node, err := ffvq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only FeatureFlagVersion ID in the query.
// Returns a *NotSingularError when more than one FeatureFlagVersion ID is found.
// Returns a *NotFoundError when no entities are found.
func (ffvq *FeatureFlagVersionQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ffvq.Limit(2).IDs(setContextOp(ctx, ffvq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{featureflagversion.Label}
	default:
		err = &NotSingularError{featureflagversion.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ffvq *FeatureFlagVersionQuery) OnlyIDX(ctx context.Context) int {
	id, err := ffvq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of FeatureFlagVersions.
func (ffvq *FeatureFlagVersionQuery) All(ctx context.Context) ([]*FeatureFlagVersion, error) {
	ctx = setContextOp(ctx, ffvq.ctx, ent.OpQueryAll)
	if err := ffvq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*FeatureFlagVersion, *FeatureFlagVersionQuery]()
	return withInterceptors[[]*FeatureFlagVersion](ctx, ffvq, qr, ffvq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ffvq *FeatureFlagVersionQuery) AllX(ctx context.Context) []*FeatureFlagVersion {
	nodes, err := ffvq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of FeatureFlagVersion IDs.
func (ffvq *FeatureFlagVersionQuery) IDs(ctx context.Context) (ids []int, err error) {
	if ffvq.ctx.Unique == nil && ffvq.path != nil {
		ffvq.Unique(true)
	}
	ctx = setContextOp(ctx, ffvq.ctx, ent.OpQueryIDs)
	if err = ffvq.Select(featureflagversion.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ffvq *FeatureFlagVersionQuery) IDsX(ctx context.Context) []int {
	ids, err := ffvq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ffvq *FeatureFlagVersionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ffvq.ctx, ent.OpQueryCount)
	if err := ffvq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ffvq, querierCount[*FeatureFlagVersionQuery](), ffvq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ffvq *FeatureFlagVersionQuery) CountX(ctx context.Context) int {
	count, err := ffvq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ffvq *FeatureFlagVersionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ffvq.ctx, ent.OpQueryExist)
	switch _, err := ffvq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ffvq *FeatureFlagVersionQuery) ExistX(ctx context.Context) bool {
	exist, err := ffvq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the FeatureFlagVersionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ffvq *FeatureFlagVersionQuery) Clone() *FeatureFlagVersionQuery {
	if ffvq == nil {
		return nil
	}
	return &FeatureFlagVersionQuery{
		config:     ffvq.config,
		ctx:        ffvq.ctx.Clone(),
		order:      append([]featureflagversion.OrderOption{}, ffvq.order...),
		inters:     append([]Interceptor{}, ffvq.inters...),
		predicates: append([]predicate.FeatureFlagVersion{}, ffvq.predicates...),
		// clone intermediate query.
		sql:  ffvq.sql.Clone(),
		path: ffvq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		WorkflowType string `json:"workflow_type,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.FeatureFlagVersion.Query().
//		GroupBy(featureflagversion.FieldWorkflowType).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ffvq *FeatureFlagVersionQuery) GroupBy(field string, fields ...string) *FeatureFlagVersionGroupBy {
	ffvq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &FeatureFlagVersionGroupBy{build: ffvq}
	grbuild.flds = &ffvq.ctx.Fields
	grbuild.label = featureflagversion.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		WorkflowType string `json:"workflow_type,omitempty"`
//	}
//
//	client.FeatureFlagVersion.Query().
//		Select(featureflagversion.FieldWorkflowType).
//		Scan(ctx, &v)
func (ffvq *FeatureFlagVersionQuery) Select(fields ...string) *FeatureFlagVersionSelect {
	ffvq.ctx.Fields = append(ffvq.ctx.Fields, fields...)
	sbuild := &FeatureFlagVersionSelect{FeatureFlagVersionQuery: ffvq}
	sbuild.label = featureflagversion.Label
	sbuild.flds, sbuild.scan = &ffvq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a FeatureFlagVersionSelect configured with the given aggregations.
func (ffvq *FeatureFlagVersionQuery) Aggregate(fns ...AggregateFunc) *FeatureFlagVersionSelect {
	return ffvq.Select().Aggregate(fns...)
}

func (ffvq *FeatureFlagVersionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ffvq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ffvq); err != nil {
				return err
			}
		}
	}
	for _, f := range ffvq.ctx.Fields {
		if !featureflagversion.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ffvq.path != nil {
		prev, err := ffvq.path(ctx)
		if err != nil {
			return err
		}
		ffvq.sql = prev
	}
	return nil
}

func (ffvq *FeatureFlagVersionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*FeatureFlagVersion, error) {
	var (
		nodes = []*FeatureFlagVersion{}
		_spec = ffvq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*FeatureFlagVersion).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &FeatureFlagVersion{config: ffvq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ffvq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (ffvq *FeatureFlagVersionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ffvq.querySpec()
	_spec.Node.Columns = ffvq.ctx.Fields
	if len(ffvq.ctx.Fields) > 0 {
		_spec.Unique = ffvq.ctx.Unique != nil && *ffvq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ffvq.driver, _spec)
}

func (ffvq *FeatureFlagVersionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(featureflagversion.Table, featureflagversion.Columns, sqlgraph.NewFieldSpec(featureflagversion.FieldID, field.TypeInt))
	_spec.From = ffvq.sql
	if unique := ffvq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ffvq.path != nil {
		_spec.Unique = true
	}
	if fields := ffvq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, featureflagversion.FieldID)
		for i := range fields {
			if fields[i] != featureflagversion.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ffvq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ffvq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ffvq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ffvq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ffvq *FeatureFlagVersionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ffvq.driver.Dialect())
	t1 := builder.Table(featureflagversion.Table)
	columns := ffvq.ctx.Fields
	if len(columns) == 0 {
		columns = featureflagversion.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ffvq.sql != nil {
		selector = ffvq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ffvq.ctx.Unique != nil && *ffvq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ffvq.predicates {
		p(selector)
	}
	for _, p := range ffvq.order {
		p(selector)
	}
	if offset := ffvq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ffvq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// FeatureFlagVersionGroupBy is the group-by builder for FeatureFlagVersion entities.
type FeatureFlagVersionGroupBy struct {
	selector
	build *FeatureFlagVersionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ffvgb *FeatureFlagVersionGroupBy) Aggregate(fns ...AggregateFunc) *FeatureFlagVersionGroupBy {
	ffvgb.fns = append(ffvgb.fns, fns...)
	return ffvgb
}

// Scan applies the selector query and scans the result into the given value.
func (ffvgb *FeatureFlagVersionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ffvgb.build.ctx, ent.OpQueryGroupBy)
	if err := ffvgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FeatureFlagVersionQuery, *FeatureFlagVersionGroupBy](ctx, ffvgb.build, ffvgb, ffvgb.build.inters, v)
}

func (ffvgb *FeatureFlagVersionGroupBy) sqlScan(ctx context.Context, root *FeatureFlagVersionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ffvgb.fns))
	for _, fn := range ffvgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ffvgb.flds)+len(ffvgb.fns))
		for _, f := range *ffvgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ffvgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ffvgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// FeatureFlagVersionSelect is the builder for selecting fields of FeatureFlagVersion entities.
type FeatureFlagVersionSelect struct {
	*FeatureFlagVersionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ffvs *FeatureFlagVersionSelect) Aggregate(fns ...AggregateFunc) *FeatureFlagVersionSelect {
	ffvs.fns = append(ffvs.fns, fns...)
	return ffvs
}

// Scan applies the selector query and scans the result into the given value.
func (ffvs *FeatureFlagVersionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ffvs.ctx, ent.OpQuerySelect)
	if err := ffvs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FeatureFlagVersionQuery, *FeatureFlagVersionSelect](ctx, ffvs.FeatureFlagVersionQuery, ffvs, ffvs.inters, v)
}

func (ffvs *FeatureFlagVersionSelect) sqlScan(ctx context.Context, root *FeatureFlagVersionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ffvs.fns))
	for _, fn := range ffvs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ffvs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ffvs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
