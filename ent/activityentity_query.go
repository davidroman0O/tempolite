// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/davidroman0O/tempolite/ent/activitydata"
	"github.com/davidroman0O/tempolite/ent/activityentity"
	"github.com/davidroman0O/tempolite/ent/activityexecution"
	"github.com/davidroman0O/tempolite/ent/predicate"
	"github.com/davidroman0O/tempolite/ent/schema"
	"github.com/davidroman0O/tempolite/ent/workflowentity"
)

// ActivityEntityQuery is the builder for querying ActivityEntity entities.
type ActivityEntityQuery struct {
	config
	ctx              *QueryContext
	order            []activityentity.OrderOption
	inters           []Interceptor
	predicates       []predicate.ActivityEntity
	withWorkflow     *WorkflowEntityQuery
	withActivityData *ActivityDataQuery
	withExecutions   *ActivityExecutionQuery
	withFKs          bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ActivityEntityQuery builder.
func (aeq *ActivityEntityQuery) Where(ps ...predicate.ActivityEntity) *ActivityEntityQuery {
	aeq.predicates = append(aeq.predicates, ps...)
	return aeq
}

// Limit the number of records to be returned by this query.
func (aeq *ActivityEntityQuery) Limit(limit int) *ActivityEntityQuery {
	aeq.ctx.Limit = &limit
	return aeq
}

// Offset to start from.
func (aeq *ActivityEntityQuery) Offset(offset int) *ActivityEntityQuery {
	aeq.ctx.Offset = &offset
	return aeq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (aeq *ActivityEntityQuery) Unique(unique bool) *ActivityEntityQuery {
	aeq.ctx.Unique = &unique
	return aeq
}

// Order specifies how the records should be ordered.
func (aeq *ActivityEntityQuery) Order(o ...activityentity.OrderOption) *ActivityEntityQuery {
	aeq.order = append(aeq.order, o...)
	return aeq
}

// QueryWorkflow chains the current query on the "workflow" edge.
func (aeq *ActivityEntityQuery) QueryWorkflow() *WorkflowEntityQuery {
	query := (&WorkflowEntityClient{config: aeq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := aeq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := aeq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(activityentity.Table, activityentity.FieldID, selector),
			sqlgraph.To(workflowentity.Table, workflowentity.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, activityentity.WorkflowTable, activityentity.WorkflowColumn),
		)
		fromU = sqlgraph.SetNeighbors(aeq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryActivityData chains the current query on the "activity_data" edge.
func (aeq *ActivityEntityQuery) QueryActivityData() *ActivityDataQuery {
	query := (&ActivityDataClient{config: aeq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := aeq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := aeq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(activityentity.Table, activityentity.FieldID, selector),
			sqlgraph.To(activitydata.Table, activitydata.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, false, activityentity.ActivityDataTable, activityentity.ActivityDataColumn),
		)
		fromU = sqlgraph.SetNeighbors(aeq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryExecutions chains the current query on the "executions" edge.
func (aeq *ActivityEntityQuery) QueryExecutions() *ActivityExecutionQuery {
	query := (&ActivityExecutionClient{config: aeq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := aeq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := aeq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(activityentity.Table, activityentity.FieldID, selector),
			sqlgraph.To(activityexecution.Table, activityexecution.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, activityentity.ExecutionsTable, activityentity.ExecutionsColumn),
		)
		fromU = sqlgraph.SetNeighbors(aeq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ActivityEntity entity from the query.
// Returns a *NotFoundError when no ActivityEntity was found.
func (aeq *ActivityEntityQuery) First(ctx context.Context) (*ActivityEntity, error) {
	nodes, err := aeq.Limit(1).All(setContextOp(ctx, aeq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{activityentity.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (aeq *ActivityEntityQuery) FirstX(ctx context.Context) *ActivityEntity {
	node, err := aeq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ActivityEntity ID from the query.
// Returns a *NotFoundError when no ActivityEntity ID was found.
func (aeq *ActivityEntityQuery) FirstID(ctx context.Context) (id schema.ActivityEntityID, err error) {
	var ids []schema.ActivityEntityID
	if ids, err = aeq.Limit(1).IDs(setContextOp(ctx, aeq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{activityentity.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (aeq *ActivityEntityQuery) FirstIDX(ctx context.Context) schema.ActivityEntityID {
	id, err := aeq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ActivityEntity entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ActivityEntity entity is found.
// Returns a *NotFoundError when no ActivityEntity entities are found.
func (aeq *ActivityEntityQuery) Only(ctx context.Context) (*ActivityEntity, error) {
	nodes, err := aeq.Limit(2).All(setContextOp(ctx, aeq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{activityentity.Label}
	default:
		return nil, &NotSingularError{activityentity.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (aeq *ActivityEntityQuery) OnlyX(ctx context.Context) *ActivityEntity {
	node, err := aeq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ActivityEntity ID in the query.
// Returns a *NotSingularError when more than one ActivityEntity ID is found.
// Returns a *NotFoundError when no entities are found.
func (aeq *ActivityEntityQuery) OnlyID(ctx context.Context) (id schema.ActivityEntityID, err error) {
	var ids []schema.ActivityEntityID
	if ids, err = aeq.Limit(2).IDs(setContextOp(ctx, aeq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{activityentity.Label}
	default:
		err = &NotSingularError{activityentity.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (aeq *ActivityEntityQuery) OnlyIDX(ctx context.Context) schema.ActivityEntityID {
	id, err := aeq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ActivityEntities.
func (aeq *ActivityEntityQuery) All(ctx context.Context) ([]*ActivityEntity, error) {
	ctx = setContextOp(ctx, aeq.ctx, ent.OpQueryAll)
	if err := aeq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ActivityEntity, *ActivityEntityQuery]()
	return withInterceptors[[]*ActivityEntity](ctx, aeq, qr, aeq.inters)
}

// AllX is like All, but panics if an error occurs.
func (aeq *ActivityEntityQuery) AllX(ctx context.Context) []*ActivityEntity {
	nodes, err := aeq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ActivityEntity IDs.
func (aeq *ActivityEntityQuery) IDs(ctx context.Context) (ids []schema.ActivityEntityID, err error) {
	if aeq.ctx.Unique == nil && aeq.path != nil {
		aeq.Unique(true)
	}
	ctx = setContextOp(ctx, aeq.ctx, ent.OpQueryIDs)
	if err = aeq.Select(activityentity.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (aeq *ActivityEntityQuery) IDsX(ctx context.Context) []schema.ActivityEntityID {
	ids, err := aeq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (aeq *ActivityEntityQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, aeq.ctx, ent.OpQueryCount)
	if err := aeq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, aeq, querierCount[*ActivityEntityQuery](), aeq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (aeq *ActivityEntityQuery) CountX(ctx context.Context) int {
	count, err := aeq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (aeq *ActivityEntityQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, aeq.ctx, ent.OpQueryExist)
	switch _, err := aeq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (aeq *ActivityEntityQuery) ExistX(ctx context.Context) bool {
	exist, err := aeq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ActivityEntityQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (aeq *ActivityEntityQuery) Clone() *ActivityEntityQuery {
	if aeq == nil {
		return nil
	}
	return &ActivityEntityQuery{
		config:           aeq.config,
		ctx:              aeq.ctx.Clone(),
		order:            append([]activityentity.OrderOption{}, aeq.order...),
		inters:           append([]Interceptor{}, aeq.inters...),
		predicates:       append([]predicate.ActivityEntity{}, aeq.predicates...),
		withWorkflow:     aeq.withWorkflow.Clone(),
		withActivityData: aeq.withActivityData.Clone(),
		withExecutions:   aeq.withExecutions.Clone(),
		// clone intermediate query.
		sql:  aeq.sql.Clone(),
		path: aeq.path,
	}
}

// WithWorkflow tells the query-builder to eager-load the nodes that are connected to
// the "workflow" edge. The optional arguments are used to configure the query builder of the edge.
func (aeq *ActivityEntityQuery) WithWorkflow(opts ...func(*WorkflowEntityQuery)) *ActivityEntityQuery {
	query := (&WorkflowEntityClient{config: aeq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	aeq.withWorkflow = query
	return aeq
}

// WithActivityData tells the query-builder to eager-load the nodes that are connected to
// the "activity_data" edge. The optional arguments are used to configure the query builder of the edge.
func (aeq *ActivityEntityQuery) WithActivityData(opts ...func(*ActivityDataQuery)) *ActivityEntityQuery {
	query := (&ActivityDataClient{config: aeq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	aeq.withActivityData = query
	return aeq
}

// WithExecutions tells the query-builder to eager-load the nodes that are connected to
// the "executions" edge. The optional arguments are used to configure the query builder of the edge.
func (aeq *ActivityEntityQuery) WithExecutions(opts ...func(*ActivityExecutionQuery)) *ActivityEntityQuery {
	query := (&ActivityExecutionClient{config: aeq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	aeq.withExecutions = query
	return aeq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		HandlerName string `json:"handler_name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ActivityEntity.Query().
//		GroupBy(activityentity.FieldHandlerName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (aeq *ActivityEntityQuery) GroupBy(field string, fields ...string) *ActivityEntityGroupBy {
	aeq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ActivityEntityGroupBy{build: aeq}
	grbuild.flds = &aeq.ctx.Fields
	grbuild.label = activityentity.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		HandlerName string `json:"handler_name,omitempty"`
//	}
//
//	client.ActivityEntity.Query().
//		Select(activityentity.FieldHandlerName).
//		Scan(ctx, &v)
func (aeq *ActivityEntityQuery) Select(fields ...string) *ActivityEntitySelect {
	aeq.ctx.Fields = append(aeq.ctx.Fields, fields...)
	sbuild := &ActivityEntitySelect{ActivityEntityQuery: aeq}
	sbuild.label = activityentity.Label
	sbuild.flds, sbuild.scan = &aeq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ActivityEntitySelect configured with the given aggregations.
func (aeq *ActivityEntityQuery) Aggregate(fns ...AggregateFunc) *ActivityEntitySelect {
	return aeq.Select().Aggregate(fns...)
}

func (aeq *ActivityEntityQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range aeq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, aeq); err != nil {
				return err
			}
		}
	}
	for _, f := range aeq.ctx.Fields {
		if !activityentity.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if aeq.path != nil {
		prev, err := aeq.path(ctx)
		if err != nil {
			return err
		}
		aeq.sql = prev
	}
	return nil
}

func (aeq *ActivityEntityQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ActivityEntity, error) {
	var (
		nodes       = []*ActivityEntity{}
		withFKs     = aeq.withFKs
		_spec       = aeq.querySpec()
		loadedTypes = [3]bool{
			aeq.withWorkflow != nil,
			aeq.withActivityData != nil,
			aeq.withExecutions != nil,
		}
	)
	if aeq.withWorkflow != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, activityentity.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ActivityEntity).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ActivityEntity{config: aeq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, aeq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := aeq.withWorkflow; query != nil {
		if err := aeq.loadWorkflow(ctx, query, nodes, nil,
			func(n *ActivityEntity, e *WorkflowEntity) { n.Edges.Workflow = e }); err != nil {
			return nil, err
		}
	}
	if query := aeq.withActivityData; query != nil {
		if err := aeq.loadActivityData(ctx, query, nodes, nil,
			func(n *ActivityEntity, e *ActivityData) { n.Edges.ActivityData = e }); err != nil {
			return nil, err
		}
	}
	if query := aeq.withExecutions; query != nil {
		if err := aeq.loadExecutions(ctx, query, nodes,
			func(n *ActivityEntity) { n.Edges.Executions = []*ActivityExecution{} },
			func(n *ActivityEntity, e *ActivityExecution) { n.Edges.Executions = append(n.Edges.Executions, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (aeq *ActivityEntityQuery) loadWorkflow(ctx context.Context, query *WorkflowEntityQuery, nodes []*ActivityEntity, init func(*ActivityEntity), assign func(*ActivityEntity, *WorkflowEntity)) error {
	ids := make([]schema.WorkflowEntityID, 0, len(nodes))
	nodeids := make(map[schema.WorkflowEntityID][]*ActivityEntity)
	for i := range nodes {
		if nodes[i].workflow_entity_activity_children == nil {
			continue
		}
		fk := *nodes[i].workflow_entity_activity_children
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(workflowentity.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "workflow_entity_activity_children" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (aeq *ActivityEntityQuery) loadActivityData(ctx context.Context, query *ActivityDataQuery, nodes []*ActivityEntity, init func(*ActivityEntity), assign func(*ActivityEntity, *ActivityData)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[schema.ActivityEntityID]*ActivityEntity)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(activitydata.FieldEntityID)
	}
	query.Where(predicate.ActivityData(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(activityentity.ActivityDataColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.EntityID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "entity_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (aeq *ActivityEntityQuery) loadExecutions(ctx context.Context, query *ActivityExecutionQuery, nodes []*ActivityEntity, init func(*ActivityEntity), assign func(*ActivityEntity, *ActivityExecution)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[schema.ActivityEntityID]*ActivityEntity)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(activityexecution.FieldActivityEntityID)
	}
	query.Where(predicate.ActivityExecution(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(activityentity.ExecutionsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ActivityEntityID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "activity_entity_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (aeq *ActivityEntityQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := aeq.querySpec()
	_spec.Node.Columns = aeq.ctx.Fields
	if len(aeq.ctx.Fields) > 0 {
		_spec.Unique = aeq.ctx.Unique != nil && *aeq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, aeq.driver, _spec)
}

func (aeq *ActivityEntityQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(activityentity.Table, activityentity.Columns, sqlgraph.NewFieldSpec(activityentity.FieldID, field.TypeInt))
	_spec.From = aeq.sql
	if unique := aeq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if aeq.path != nil {
		_spec.Unique = true
	}
	if fields := aeq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, activityentity.FieldID)
		for i := range fields {
			if fields[i] != activityentity.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := aeq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := aeq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := aeq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := aeq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (aeq *ActivityEntityQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(aeq.driver.Dialect())
	t1 := builder.Table(activityentity.Table)
	columns := aeq.ctx.Fields
	if len(columns) == 0 {
		columns = activityentity.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if aeq.sql != nil {
		selector = aeq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if aeq.ctx.Unique != nil && *aeq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range aeq.predicates {
		p(selector)
	}
	for _, p := range aeq.order {
		p(selector)
	}
	if offset := aeq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := aeq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ActivityEntityGroupBy is the group-by builder for ActivityEntity entities.
type ActivityEntityGroupBy struct {
	selector
	build *ActivityEntityQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (aegb *ActivityEntityGroupBy) Aggregate(fns ...AggregateFunc) *ActivityEntityGroupBy {
	aegb.fns = append(aegb.fns, fns...)
	return aegb
}

// Scan applies the selector query and scans the result into the given value.
func (aegb *ActivityEntityGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, aegb.build.ctx, ent.OpQueryGroupBy)
	if err := aegb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ActivityEntityQuery, *ActivityEntityGroupBy](ctx, aegb.build, aegb, aegb.build.inters, v)
}

func (aegb *ActivityEntityGroupBy) sqlScan(ctx context.Context, root *ActivityEntityQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(aegb.fns))
	for _, fn := range aegb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*aegb.flds)+len(aegb.fns))
		for _, f := range *aegb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*aegb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := aegb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ActivityEntitySelect is the builder for selecting fields of ActivityEntity entities.
type ActivityEntitySelect struct {
	*ActivityEntityQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (aes *ActivityEntitySelect) Aggregate(fns ...AggregateFunc) *ActivityEntitySelect {
	aes.fns = append(aes.fns, fns...)
	return aes
}

// Scan applies the selector query and scans the result into the given value.
func (aes *ActivityEntitySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, aes.ctx, ent.OpQuerySelect)
	if err := aes.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ActivityEntityQuery, *ActivityEntitySelect](ctx, aes.ActivityEntityQuery, aes, aes.inters, v)
}

func (aes *ActivityEntitySelect) sqlScan(ctx context.Context, root *ActivityEntityQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(aes.fns))
	for _, fn := range aes.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*aes.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := aes.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
