// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/davidroman0O/go-tempolite/ent/sagatask"
)

// SagaTask is the model entity for the SagaTask schema.
type SagaTask struct {
	config
	// ID of the ent.
	ID           int `json:"id,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SagaTask) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case sagatask.FieldID:
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SagaTask fields.
func (st *SagaTask) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case sagatask.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			st.ID = int(value.Int64)
		default:
			st.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SagaTask.
// This includes values selected through modifiers, order, etc.
func (st *SagaTask) Value(name string) (ent.Value, error) {
	return st.selectValues.Get(name)
}

// Update returns a builder for updating this SagaTask.
// Note that you need to call SagaTask.Unwrap() before calling this method if this SagaTask
// was returned from a transaction, and the transaction was committed or rolled back.
func (st *SagaTask) Update() *SagaTaskUpdateOne {
	return NewSagaTaskClient(st.config).UpdateOne(st)
}

// Unwrap unwraps the SagaTask entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (st *SagaTask) Unwrap() *SagaTask {
	_tx, ok := st.config.driver.(*txDriver)
	if !ok {
		panic("ent: SagaTask is not a transactional entity")
	}
	st.config.driver = _tx.drv
	return st
}

// String implements the fmt.Stringer.
func (st *SagaTask) String() string {
	var builder strings.Builder
	builder.WriteString("SagaTask(")
	builder.WriteString(fmt.Sprintf("id=%v", st.ID))
	builder.WriteByte(')')
	return builder.String()
}

// SagaTasks is a parsable slice of SagaTask.
type SagaTasks []*SagaTask
