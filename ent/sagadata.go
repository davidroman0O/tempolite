// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/davidroman0O/tempolite/ent/sagadata"
	"github.com/davidroman0O/tempolite/ent/sagaentity"
	"github.com/davidroman0O/tempolite/ent/schema"
)

// SagaData is the model entity for the SagaData schema.
type SagaData struct {
	config `json:"-"`
	// ID of the ent.
	ID schema.SagaDataID `json:"id,omitempty"`
	// EntityID holds the value of the "entity_id" field.
	EntityID schema.SagaEntityID `json:"entity_id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SagaDataQuery when eager-loading is set.
	Edges        SagaDataEdges `json:"edges"`
	selectValues sql.SelectValues
}

// SagaDataEdges holds the relations/edges for other nodes in the graph.
type SagaDataEdges struct {
	// Saga holds the value of the saga edge.
	Saga *SagaEntity `json:"saga,omitempty"`
	// Values holds the value of the values edge.
	Values []*SagaValue `json:"values,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// SagaOrErr returns the Saga value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SagaDataEdges) SagaOrErr() (*SagaEntity, error) {
	if e.Saga != nil {
		return e.Saga, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: sagaentity.Label}
	}
	return nil, &NotLoadedError{edge: "saga"}
}

// ValuesOrErr returns the Values value or an error if the edge
// was not loaded in eager-loading.
func (e SagaDataEdges) ValuesOrErr() ([]*SagaValue, error) {
	if e.loadedTypes[1] {
		return e.Values, nil
	}
	return nil, &NotLoadedError{edge: "values"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SagaData) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case sagadata.FieldID, sagadata.FieldEntityID:
			values[i] = new(sql.NullInt64)
		case sagadata.FieldCreatedAt, sagadata.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SagaData fields.
func (sd *SagaData) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case sagadata.FieldID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				sd.ID = schema.SagaDataID(value.Int64)
			}
		case sagadata.FieldEntityID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field entity_id", values[i])
			} else if value.Valid {
				sd.EntityID = schema.SagaEntityID(value.Int64)
			}
		case sagadata.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				sd.CreatedAt = value.Time
			}
		case sagadata.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				sd.UpdatedAt = value.Time
			}
		default:
			sd.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SagaData.
// This includes values selected through modifiers, order, etc.
func (sd *SagaData) Value(name string) (ent.Value, error) {
	return sd.selectValues.Get(name)
}

// QuerySaga queries the "saga" edge of the SagaData entity.
func (sd *SagaData) QuerySaga() *SagaEntityQuery {
	return NewSagaDataClient(sd.config).QuerySaga(sd)
}

// QueryValues queries the "values" edge of the SagaData entity.
func (sd *SagaData) QueryValues() *SagaValueQuery {
	return NewSagaDataClient(sd.config).QueryValues(sd)
}

// Update returns a builder for updating this SagaData.
// Note that you need to call SagaData.Unwrap() before calling this method if this SagaData
// was returned from a transaction, and the transaction was committed or rolled back.
func (sd *SagaData) Update() *SagaDataUpdateOne {
	return NewSagaDataClient(sd.config).UpdateOne(sd)
}

// Unwrap unwraps the SagaData entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (sd *SagaData) Unwrap() *SagaData {
	_tx, ok := sd.config.driver.(*txDriver)
	if !ok {
		panic("ent: SagaData is not a transactional entity")
	}
	sd.config.driver = _tx.drv
	return sd
}

// String implements the fmt.Stringer.
func (sd *SagaData) String() string {
	var builder strings.Builder
	builder.WriteString("SagaData(")
	builder.WriteString(fmt.Sprintf("id=%v, ", sd.ID))
	builder.WriteString("entity_id=")
	builder.WriteString(fmt.Sprintf("%v", sd.EntityID))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(sd.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(sd.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// SagaDataSlice is a parsable slice of SagaData.
type SagaDataSlice []*SagaData
