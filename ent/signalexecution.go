// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/davidroman0O/tempolite/ent/schema"
	"github.com/davidroman0O/tempolite/ent/signalentity"
	"github.com/davidroman0O/tempolite/ent/signalexecution"
	"github.com/davidroman0O/tempolite/ent/signalexecutiondata"
)

// SignalExecution is the model entity for the SignalExecution schema.
type SignalExecution struct {
	config `json:"-"`
	// ID of the ent.
	ID schema.SignalExecutionID `json:"id,omitempty"`
	// EntityID holds the value of the "entity_id" field.
	EntityID schema.SignalEntityID `json:"entity_id,omitempty"`
	// StartedAt holds the value of the "started_at" field.
	StartedAt time.Time `json:"started_at,omitempty"`
	// CompletedAt holds the value of the "completed_at" field.
	CompletedAt *time.Time `json:"completed_at,omitempty"`
	// Status holds the value of the "status" field.
	Status schema.ExecutionStatus `json:"status,omitempty"`
	// Error holds the value of the "error" field.
	Error string `json:"error,omitempty"`
	// StackTrace holds the value of the "stack_trace" field.
	StackTrace string `json:"stack_trace,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SignalExecutionQuery when eager-loading is set.
	Edges        SignalExecutionEdges `json:"edges"`
	selectValues sql.SelectValues
}

// SignalExecutionEdges holds the relations/edges for other nodes in the graph.
type SignalExecutionEdges struct {
	// Signal holds the value of the signal edge.
	Signal *SignalEntity `json:"signal,omitempty"`
	// ExecutionData holds the value of the execution_data edge.
	ExecutionData *SignalExecutionData `json:"execution_data,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// SignalOrErr returns the Signal value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SignalExecutionEdges) SignalOrErr() (*SignalEntity, error) {
	if e.Signal != nil {
		return e.Signal, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: signalentity.Label}
	}
	return nil, &NotLoadedError{edge: "signal"}
}

// ExecutionDataOrErr returns the ExecutionData value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SignalExecutionEdges) ExecutionDataOrErr() (*SignalExecutionData, error) {
	if e.ExecutionData != nil {
		return e.ExecutionData, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: signalexecutiondata.Label}
	}
	return nil, &NotLoadedError{edge: "execution_data"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SignalExecution) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case signalexecution.FieldID, signalexecution.FieldEntityID:
			values[i] = new(sql.NullInt64)
		case signalexecution.FieldStatus, signalexecution.FieldError, signalexecution.FieldStackTrace:
			values[i] = new(sql.NullString)
		case signalexecution.FieldStartedAt, signalexecution.FieldCompletedAt, signalexecution.FieldCreatedAt, signalexecution.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SignalExecution fields.
func (se *SignalExecution) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case signalexecution.FieldID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				se.ID = schema.SignalExecutionID(value.Int64)
			}
		case signalexecution.FieldEntityID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field entity_id", values[i])
			} else if value.Valid {
				se.EntityID = schema.SignalEntityID(value.Int64)
			}
		case signalexecution.FieldStartedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field started_at", values[i])
			} else if value.Valid {
				se.StartedAt = value.Time
			}
		case signalexecution.FieldCompletedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field completed_at", values[i])
			} else if value.Valid {
				se.CompletedAt = new(time.Time)
				*se.CompletedAt = value.Time
			}
		case signalexecution.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				se.Status = schema.ExecutionStatus(value.String)
			}
		case signalexecution.FieldError:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field error", values[i])
			} else if value.Valid {
				se.Error = value.String
			}
		case signalexecution.FieldStackTrace:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field stack_trace", values[i])
			} else if value.Valid {
				se.StackTrace = value.String
			}
		case signalexecution.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				se.CreatedAt = value.Time
			}
		case signalexecution.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				se.UpdatedAt = value.Time
			}
		default:
			se.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SignalExecution.
// This includes values selected through modifiers, order, etc.
func (se *SignalExecution) Value(name string) (ent.Value, error) {
	return se.selectValues.Get(name)
}

// QuerySignal queries the "signal" edge of the SignalExecution entity.
func (se *SignalExecution) QuerySignal() *SignalEntityQuery {
	return NewSignalExecutionClient(se.config).QuerySignal(se)
}

// QueryExecutionData queries the "execution_data" edge of the SignalExecution entity.
func (se *SignalExecution) QueryExecutionData() *SignalExecutionDataQuery {
	return NewSignalExecutionClient(se.config).QueryExecutionData(se)
}

// Update returns a builder for updating this SignalExecution.
// Note that you need to call SignalExecution.Unwrap() before calling this method if this SignalExecution
// was returned from a transaction, and the transaction was committed or rolled back.
func (se *SignalExecution) Update() *SignalExecutionUpdateOne {
	return NewSignalExecutionClient(se.config).UpdateOne(se)
}

// Unwrap unwraps the SignalExecution entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (se *SignalExecution) Unwrap() *SignalExecution {
	_tx, ok := se.config.driver.(*txDriver)
	if !ok {
		panic("ent: SignalExecution is not a transactional entity")
	}
	se.config.driver = _tx.drv
	return se
}

// String implements the fmt.Stringer.
func (se *SignalExecution) String() string {
	var builder strings.Builder
	builder.WriteString("SignalExecution(")
	builder.WriteString(fmt.Sprintf("id=%v, ", se.ID))
	builder.WriteString("entity_id=")
	builder.WriteString(fmt.Sprintf("%v", se.EntityID))
	builder.WriteString(", ")
	builder.WriteString("started_at=")
	builder.WriteString(se.StartedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	if v := se.CompletedAt; v != nil {
		builder.WriteString("completed_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(fmt.Sprintf("%v", se.Status))
	builder.WriteString(", ")
	builder.WriteString("error=")
	builder.WriteString(se.Error)
	builder.WriteString(", ")
	builder.WriteString("stack_trace=")
	builder.WriteString(se.StackTrace)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(se.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(se.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// SignalExecutions is a parsable slice of SignalExecution.
type SignalExecutions []*SignalExecution
