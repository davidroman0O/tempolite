// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/davidroman0O/tempolite/ent/activitydata"
	"github.com/davidroman0O/tempolite/ent/activityentity"
	"github.com/davidroman0O/tempolite/ent/predicate"
	"github.com/davidroman0O/tempolite/ent/schema"
)

// ActivityDataQuery is the builder for querying ActivityData entities.
type ActivityDataQuery struct {
	config
	ctx          *QueryContext
	order        []activitydata.OrderOption
	inters       []Interceptor
	predicates   []predicate.ActivityData
	withActivity *ActivityEntityQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ActivityDataQuery builder.
func (adq *ActivityDataQuery) Where(ps ...predicate.ActivityData) *ActivityDataQuery {
	adq.predicates = append(adq.predicates, ps...)
	return adq
}

// Limit the number of records to be returned by this query.
func (adq *ActivityDataQuery) Limit(limit int) *ActivityDataQuery {
	adq.ctx.Limit = &limit
	return adq
}

// Offset to start from.
func (adq *ActivityDataQuery) Offset(offset int) *ActivityDataQuery {
	adq.ctx.Offset = &offset
	return adq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (adq *ActivityDataQuery) Unique(unique bool) *ActivityDataQuery {
	adq.ctx.Unique = &unique
	return adq
}

// Order specifies how the records should be ordered.
func (adq *ActivityDataQuery) Order(o ...activitydata.OrderOption) *ActivityDataQuery {
	adq.order = append(adq.order, o...)
	return adq
}

// QueryActivity chains the current query on the "activity" edge.
func (adq *ActivityDataQuery) QueryActivity() *ActivityEntityQuery {
	query := (&ActivityEntityClient{config: adq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := adq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := adq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(activitydata.Table, activitydata.FieldID, selector),
			sqlgraph.To(activityentity.Table, activityentity.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, activitydata.ActivityTable, activitydata.ActivityColumn),
		)
		fromU = sqlgraph.SetNeighbors(adq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ActivityData entity from the query.
// Returns a *NotFoundError when no ActivityData was found.
func (adq *ActivityDataQuery) First(ctx context.Context) (*ActivityData, error) {
	nodes, err := adq.Limit(1).All(setContextOp(ctx, adq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{activitydata.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (adq *ActivityDataQuery) FirstX(ctx context.Context) *ActivityData {
	node, err := adq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ActivityData ID from the query.
// Returns a *NotFoundError when no ActivityData ID was found.
func (adq *ActivityDataQuery) FirstID(ctx context.Context) (id schema.ActivityDataID, err error) {
	var ids []schema.ActivityDataID
	if ids, err = adq.Limit(1).IDs(setContextOp(ctx, adq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{activitydata.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (adq *ActivityDataQuery) FirstIDX(ctx context.Context) schema.ActivityDataID {
	id, err := adq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ActivityData entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ActivityData entity is found.
// Returns a *NotFoundError when no ActivityData entities are found.
func (adq *ActivityDataQuery) Only(ctx context.Context) (*ActivityData, error) {
	nodes, err := adq.Limit(2).All(setContextOp(ctx, adq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{activitydata.Label}
	default:
		return nil, &NotSingularError{activitydata.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (adq *ActivityDataQuery) OnlyX(ctx context.Context) *ActivityData {
	node, err := adq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ActivityData ID in the query.
// Returns a *NotSingularError when more than one ActivityData ID is found.
// Returns a *NotFoundError when no entities are found.
func (adq *ActivityDataQuery) OnlyID(ctx context.Context) (id schema.ActivityDataID, err error) {
	var ids []schema.ActivityDataID
	if ids, err = adq.Limit(2).IDs(setContextOp(ctx, adq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{activitydata.Label}
	default:
		err = &NotSingularError{activitydata.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (adq *ActivityDataQuery) OnlyIDX(ctx context.Context) schema.ActivityDataID {
	id, err := adq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ActivityDataSlice.
func (adq *ActivityDataQuery) All(ctx context.Context) ([]*ActivityData, error) {
	ctx = setContextOp(ctx, adq.ctx, ent.OpQueryAll)
	if err := adq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ActivityData, *ActivityDataQuery]()
	return withInterceptors[[]*ActivityData](ctx, adq, qr, adq.inters)
}

// AllX is like All, but panics if an error occurs.
func (adq *ActivityDataQuery) AllX(ctx context.Context) []*ActivityData {
	nodes, err := adq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ActivityData IDs.
func (adq *ActivityDataQuery) IDs(ctx context.Context) (ids []schema.ActivityDataID, err error) {
	if adq.ctx.Unique == nil && adq.path != nil {
		adq.Unique(true)
	}
	ctx = setContextOp(ctx, adq.ctx, ent.OpQueryIDs)
	if err = adq.Select(activitydata.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (adq *ActivityDataQuery) IDsX(ctx context.Context) []schema.ActivityDataID {
	ids, err := adq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (adq *ActivityDataQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, adq.ctx, ent.OpQueryCount)
	if err := adq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, adq, querierCount[*ActivityDataQuery](), adq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (adq *ActivityDataQuery) CountX(ctx context.Context) int {
	count, err := adq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (adq *ActivityDataQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, adq.ctx, ent.OpQueryExist)
	switch _, err := adq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (adq *ActivityDataQuery) ExistX(ctx context.Context) bool {
	exist, err := adq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ActivityDataQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (adq *ActivityDataQuery) Clone() *ActivityDataQuery {
	if adq == nil {
		return nil
	}
	return &ActivityDataQuery{
		config:       adq.config,
		ctx:          adq.ctx.Clone(),
		order:        append([]activitydata.OrderOption{}, adq.order...),
		inters:       append([]Interceptor{}, adq.inters...),
		predicates:   append([]predicate.ActivityData{}, adq.predicates...),
		withActivity: adq.withActivity.Clone(),
		// clone intermediate query.
		sql:  adq.sql.Clone(),
		path: adq.path,
	}
}

// WithActivity tells the query-builder to eager-load the nodes that are connected to
// the "activity" edge. The optional arguments are used to configure the query builder of the edge.
func (adq *ActivityDataQuery) WithActivity(opts ...func(*ActivityEntityQuery)) *ActivityDataQuery {
	query := (&ActivityEntityClient{config: adq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	adq.withActivity = query
	return adq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		EntityID schema.ActivityEntityID `json:"entity_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ActivityData.Query().
//		GroupBy(activitydata.FieldEntityID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (adq *ActivityDataQuery) GroupBy(field string, fields ...string) *ActivityDataGroupBy {
	adq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ActivityDataGroupBy{build: adq}
	grbuild.flds = &adq.ctx.Fields
	grbuild.label = activitydata.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		EntityID schema.ActivityEntityID `json:"entity_id,omitempty"`
//	}
//
//	client.ActivityData.Query().
//		Select(activitydata.FieldEntityID).
//		Scan(ctx, &v)
func (adq *ActivityDataQuery) Select(fields ...string) *ActivityDataSelect {
	adq.ctx.Fields = append(adq.ctx.Fields, fields...)
	sbuild := &ActivityDataSelect{ActivityDataQuery: adq}
	sbuild.label = activitydata.Label
	sbuild.flds, sbuild.scan = &adq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ActivityDataSelect configured with the given aggregations.
func (adq *ActivityDataQuery) Aggregate(fns ...AggregateFunc) *ActivityDataSelect {
	return adq.Select().Aggregate(fns...)
}

func (adq *ActivityDataQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range adq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, adq); err != nil {
				return err
			}
		}
	}
	for _, f := range adq.ctx.Fields {
		if !activitydata.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if adq.path != nil {
		prev, err := adq.path(ctx)
		if err != nil {
			return err
		}
		adq.sql = prev
	}
	return nil
}

func (adq *ActivityDataQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ActivityData, error) {
	var (
		nodes       = []*ActivityData{}
		_spec       = adq.querySpec()
		loadedTypes = [1]bool{
			adq.withActivity != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ActivityData).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ActivityData{config: adq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, adq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := adq.withActivity; query != nil {
		if err := adq.loadActivity(ctx, query, nodes, nil,
			func(n *ActivityData, e *ActivityEntity) { n.Edges.Activity = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (adq *ActivityDataQuery) loadActivity(ctx context.Context, query *ActivityEntityQuery, nodes []*ActivityData, init func(*ActivityData), assign func(*ActivityData, *ActivityEntity)) error {
	ids := make([]schema.ActivityEntityID, 0, len(nodes))
	nodeids := make(map[schema.ActivityEntityID][]*ActivityData)
	for i := range nodes {
		fk := nodes[i].EntityID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(activityentity.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "entity_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (adq *ActivityDataQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := adq.querySpec()
	_spec.Node.Columns = adq.ctx.Fields
	if len(adq.ctx.Fields) > 0 {
		_spec.Unique = adq.ctx.Unique != nil && *adq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, adq.driver, _spec)
}

func (adq *ActivityDataQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(activitydata.Table, activitydata.Columns, sqlgraph.NewFieldSpec(activitydata.FieldID, field.TypeInt))
	_spec.From = adq.sql
	if unique := adq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if adq.path != nil {
		_spec.Unique = true
	}
	if fields := adq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, activitydata.FieldID)
		for i := range fields {
			if fields[i] != activitydata.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if adq.withActivity != nil {
			_spec.Node.AddColumnOnce(activitydata.FieldEntityID)
		}
	}
	if ps := adq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := adq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := adq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := adq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (adq *ActivityDataQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(adq.driver.Dialect())
	t1 := builder.Table(activitydata.Table)
	columns := adq.ctx.Fields
	if len(columns) == 0 {
		columns = activitydata.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if adq.sql != nil {
		selector = adq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if adq.ctx.Unique != nil && *adq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range adq.predicates {
		p(selector)
	}
	for _, p := range adq.order {
		p(selector)
	}
	if offset := adq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := adq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ActivityDataGroupBy is the group-by builder for ActivityData entities.
type ActivityDataGroupBy struct {
	selector
	build *ActivityDataQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (adgb *ActivityDataGroupBy) Aggregate(fns ...AggregateFunc) *ActivityDataGroupBy {
	adgb.fns = append(adgb.fns, fns...)
	return adgb
}

// Scan applies the selector query and scans the result into the given value.
func (adgb *ActivityDataGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, adgb.build.ctx, ent.OpQueryGroupBy)
	if err := adgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ActivityDataQuery, *ActivityDataGroupBy](ctx, adgb.build, adgb, adgb.build.inters, v)
}

func (adgb *ActivityDataGroupBy) sqlScan(ctx context.Context, root *ActivityDataQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(adgb.fns))
	for _, fn := range adgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*adgb.flds)+len(adgb.fns))
		for _, f := range *adgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*adgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := adgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ActivityDataSelect is the builder for selecting fields of ActivityData entities.
type ActivityDataSelect struct {
	*ActivityDataQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ads *ActivityDataSelect) Aggregate(fns ...AggregateFunc) *ActivityDataSelect {
	ads.fns = append(ads.fns, fns...)
	return ads
}

// Scan applies the selector query and scans the result into the given value.
func (ads *ActivityDataSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ads.ctx, ent.OpQuerySelect)
	if err := ads.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ActivityDataQuery, *ActivityDataSelect](ctx, ads.ActivityDataQuery, ads, ads.inters, v)
}

func (ads *ActivityDataSelect) sqlScan(ctx context.Context, root *ActivityDataQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ads.fns))
	for _, fn := range ads.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ads.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ads.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
