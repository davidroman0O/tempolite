// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/davidroman0O/go-tempolite/ent/executionunit"
	"github.com/davidroman0O/go-tempolite/ent/predicate"
	"github.com/davidroman0O/go-tempolite/ent/sagacompensation"
	"github.com/davidroman0O/go-tempolite/ent/sagatransaction"
	"github.com/davidroman0O/go-tempolite/ent/task"
)

// SagaCompensationQuery is the builder for querying SagaCompensation entities.
type SagaCompensationQuery struct {
	config
	ctx               *QueryContext
	order             []sagacompensation.OrderOption
	inters            []Interceptor
	predicates        []predicate.SagaCompensation
	withExecutionUnit *ExecutionUnitQuery
	withTask          *TaskQuery
	withTransaction   *SagaTransactionQuery
	withFKs           bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SagaCompensationQuery builder.
func (scq *SagaCompensationQuery) Where(ps ...predicate.SagaCompensation) *SagaCompensationQuery {
	scq.predicates = append(scq.predicates, ps...)
	return scq
}

// Limit the number of records to be returned by this query.
func (scq *SagaCompensationQuery) Limit(limit int) *SagaCompensationQuery {
	scq.ctx.Limit = &limit
	return scq
}

// Offset to start from.
func (scq *SagaCompensationQuery) Offset(offset int) *SagaCompensationQuery {
	scq.ctx.Offset = &offset
	return scq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (scq *SagaCompensationQuery) Unique(unique bool) *SagaCompensationQuery {
	scq.ctx.Unique = &unique
	return scq
}

// Order specifies how the records should be ordered.
func (scq *SagaCompensationQuery) Order(o ...sagacompensation.OrderOption) *SagaCompensationQuery {
	scq.order = append(scq.order, o...)
	return scq
}

// QueryExecutionUnit chains the current query on the "execution_unit" edge.
func (scq *SagaCompensationQuery) QueryExecutionUnit() *ExecutionUnitQuery {
	query := (&ExecutionUnitClient{config: scq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := scq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := scq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(sagacompensation.Table, sagacompensation.FieldID, selector),
			sqlgraph.To(executionunit.Table, executionunit.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, sagacompensation.ExecutionUnitTable, sagacompensation.ExecutionUnitColumn),
		)
		fromU = sqlgraph.SetNeighbors(scq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTask chains the current query on the "task" edge.
func (scq *SagaCompensationQuery) QueryTask() *TaskQuery {
	query := (&TaskClient{config: scq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := scq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := scq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(sagacompensation.Table, sagacompensation.FieldID, selector),
			sqlgraph.To(task.Table, task.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, sagacompensation.TaskTable, sagacompensation.TaskColumn),
		)
		fromU = sqlgraph.SetNeighbors(scq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTransaction chains the current query on the "transaction" edge.
func (scq *SagaCompensationQuery) QueryTransaction() *SagaTransactionQuery {
	query := (&SagaTransactionClient{config: scq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := scq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := scq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(sagacompensation.Table, sagacompensation.FieldID, selector),
			sqlgraph.To(sagatransaction.Table, sagatransaction.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, sagacompensation.TransactionTable, sagacompensation.TransactionColumn),
		)
		fromU = sqlgraph.SetNeighbors(scq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first SagaCompensation entity from the query.
// Returns a *NotFoundError when no SagaCompensation was found.
func (scq *SagaCompensationQuery) First(ctx context.Context) (*SagaCompensation, error) {
	nodes, err := scq.Limit(1).All(setContextOp(ctx, scq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{sagacompensation.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (scq *SagaCompensationQuery) FirstX(ctx context.Context) *SagaCompensation {
	node, err := scq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SagaCompensation ID from the query.
// Returns a *NotFoundError when no SagaCompensation ID was found.
func (scq *SagaCompensationQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = scq.Limit(1).IDs(setContextOp(ctx, scq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{sagacompensation.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (scq *SagaCompensationQuery) FirstIDX(ctx context.Context) string {
	id, err := scq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SagaCompensation entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SagaCompensation entity is found.
// Returns a *NotFoundError when no SagaCompensation entities are found.
func (scq *SagaCompensationQuery) Only(ctx context.Context) (*SagaCompensation, error) {
	nodes, err := scq.Limit(2).All(setContextOp(ctx, scq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{sagacompensation.Label}
	default:
		return nil, &NotSingularError{sagacompensation.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (scq *SagaCompensationQuery) OnlyX(ctx context.Context) *SagaCompensation {
	node, err := scq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SagaCompensation ID in the query.
// Returns a *NotSingularError when more than one SagaCompensation ID is found.
// Returns a *NotFoundError when no entities are found.
func (scq *SagaCompensationQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = scq.Limit(2).IDs(setContextOp(ctx, scq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{sagacompensation.Label}
	default:
		err = &NotSingularError{sagacompensation.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (scq *SagaCompensationQuery) OnlyIDX(ctx context.Context) string {
	id, err := scq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SagaCompensations.
func (scq *SagaCompensationQuery) All(ctx context.Context) ([]*SagaCompensation, error) {
	ctx = setContextOp(ctx, scq.ctx, ent.OpQueryAll)
	if err := scq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SagaCompensation, *SagaCompensationQuery]()
	return withInterceptors[[]*SagaCompensation](ctx, scq, qr, scq.inters)
}

// AllX is like All, but panics if an error occurs.
func (scq *SagaCompensationQuery) AllX(ctx context.Context) []*SagaCompensation {
	nodes, err := scq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SagaCompensation IDs.
func (scq *SagaCompensationQuery) IDs(ctx context.Context) (ids []string, err error) {
	if scq.ctx.Unique == nil && scq.path != nil {
		scq.Unique(true)
	}
	ctx = setContextOp(ctx, scq.ctx, ent.OpQueryIDs)
	if err = scq.Select(sagacompensation.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (scq *SagaCompensationQuery) IDsX(ctx context.Context) []string {
	ids, err := scq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (scq *SagaCompensationQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, scq.ctx, ent.OpQueryCount)
	if err := scq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, scq, querierCount[*SagaCompensationQuery](), scq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (scq *SagaCompensationQuery) CountX(ctx context.Context) int {
	count, err := scq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (scq *SagaCompensationQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, scq.ctx, ent.OpQueryExist)
	switch _, err := scq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (scq *SagaCompensationQuery) ExistX(ctx context.Context) bool {
	exist, err := scq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SagaCompensationQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (scq *SagaCompensationQuery) Clone() *SagaCompensationQuery {
	if scq == nil {
		return nil
	}
	return &SagaCompensationQuery{
		config:            scq.config,
		ctx:               scq.ctx.Clone(),
		order:             append([]sagacompensation.OrderOption{}, scq.order...),
		inters:            append([]Interceptor{}, scq.inters...),
		predicates:        append([]predicate.SagaCompensation{}, scq.predicates...),
		withExecutionUnit: scq.withExecutionUnit.Clone(),
		withTask:          scq.withTask.Clone(),
		withTransaction:   scq.withTransaction.Clone(),
		// clone intermediate query.
		sql:  scq.sql.Clone(),
		path: scq.path,
	}
}

// WithExecutionUnit tells the query-builder to eager-load the nodes that are connected to
// the "execution_unit" edge. The optional arguments are used to configure the query builder of the edge.
func (scq *SagaCompensationQuery) WithExecutionUnit(opts ...func(*ExecutionUnitQuery)) *SagaCompensationQuery {
	query := (&ExecutionUnitClient{config: scq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	scq.withExecutionUnit = query
	return scq
}

// WithTask tells the query-builder to eager-load the nodes that are connected to
// the "task" edge. The optional arguments are used to configure the query builder of the edge.
func (scq *SagaCompensationQuery) WithTask(opts ...func(*TaskQuery)) *SagaCompensationQuery {
	query := (&TaskClient{config: scq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	scq.withTask = query
	return scq
}

// WithTransaction tells the query-builder to eager-load the nodes that are connected to
// the "transaction" edge. The optional arguments are used to configure the query builder of the edge.
func (scq *SagaCompensationQuery) WithTransaction(opts ...func(*SagaTransactionQuery)) *SagaCompensationQuery {
	query := (&SagaTransactionClient{config: scq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	scq.withTransaction = query
	return scq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Order int `json:"order,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SagaCompensation.Query().
//		GroupBy(sagacompensation.FieldOrder).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (scq *SagaCompensationQuery) GroupBy(field string, fields ...string) *SagaCompensationGroupBy {
	scq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SagaCompensationGroupBy{build: scq}
	grbuild.flds = &scq.ctx.Fields
	grbuild.label = sagacompensation.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Order int `json:"order,omitempty"`
//	}
//
//	client.SagaCompensation.Query().
//		Select(sagacompensation.FieldOrder).
//		Scan(ctx, &v)
func (scq *SagaCompensationQuery) Select(fields ...string) *SagaCompensationSelect {
	scq.ctx.Fields = append(scq.ctx.Fields, fields...)
	sbuild := &SagaCompensationSelect{SagaCompensationQuery: scq}
	sbuild.label = sagacompensation.Label
	sbuild.flds, sbuild.scan = &scq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SagaCompensationSelect configured with the given aggregations.
func (scq *SagaCompensationQuery) Aggregate(fns ...AggregateFunc) *SagaCompensationSelect {
	return scq.Select().Aggregate(fns...)
}

func (scq *SagaCompensationQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range scq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, scq); err != nil {
				return err
			}
		}
	}
	for _, f := range scq.ctx.Fields {
		if !sagacompensation.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if scq.path != nil {
		prev, err := scq.path(ctx)
		if err != nil {
			return err
		}
		scq.sql = prev
	}
	return nil
}

func (scq *SagaCompensationQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SagaCompensation, error) {
	var (
		nodes       = []*SagaCompensation{}
		withFKs     = scq.withFKs
		_spec       = scq.querySpec()
		loadedTypes = [3]bool{
			scq.withExecutionUnit != nil,
			scq.withTask != nil,
			scq.withTransaction != nil,
		}
	)
	if scq.withExecutionUnit != nil || scq.withTask != nil || scq.withTransaction != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, sagacompensation.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SagaCompensation).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SagaCompensation{config: scq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, scq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := scq.withExecutionUnit; query != nil {
		if err := scq.loadExecutionUnit(ctx, query, nodes, nil,
			func(n *SagaCompensation, e *ExecutionUnit) { n.Edges.ExecutionUnit = e }); err != nil {
			return nil, err
		}
	}
	if query := scq.withTask; query != nil {
		if err := scq.loadTask(ctx, query, nodes, nil,
			func(n *SagaCompensation, e *Task) { n.Edges.Task = e }); err != nil {
			return nil, err
		}
	}
	if query := scq.withTransaction; query != nil {
		if err := scq.loadTransaction(ctx, query, nodes, nil,
			func(n *SagaCompensation, e *SagaTransaction) { n.Edges.Transaction = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (scq *SagaCompensationQuery) loadExecutionUnit(ctx context.Context, query *ExecutionUnitQuery, nodes []*SagaCompensation, init func(*SagaCompensation), assign func(*SagaCompensation, *ExecutionUnit)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*SagaCompensation)
	for i := range nodes {
		if nodes[i].execution_unit_saga_compensations == nil {
			continue
		}
		fk := *nodes[i].execution_unit_saga_compensations
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(executionunit.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "execution_unit_saga_compensations" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (scq *SagaCompensationQuery) loadTask(ctx context.Context, query *TaskQuery, nodes []*SagaCompensation, init func(*SagaCompensation), assign func(*SagaCompensation, *Task)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*SagaCompensation)
	for i := range nodes {
		if nodes[i].saga_compensation_task == nil {
			continue
		}
		fk := *nodes[i].saga_compensation_task
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(task.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "saga_compensation_task" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (scq *SagaCompensationQuery) loadTransaction(ctx context.Context, query *SagaTransactionQuery, nodes []*SagaCompensation, init func(*SagaCompensation), assign func(*SagaCompensation, *SagaTransaction)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*SagaCompensation)
	for i := range nodes {
		if nodes[i].saga_transaction_compensation == nil {
			continue
		}
		fk := *nodes[i].saga_transaction_compensation
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(sagatransaction.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "saga_transaction_compensation" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (scq *SagaCompensationQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := scq.querySpec()
	_spec.Node.Columns = scq.ctx.Fields
	if len(scq.ctx.Fields) > 0 {
		_spec.Unique = scq.ctx.Unique != nil && *scq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, scq.driver, _spec)
}

func (scq *SagaCompensationQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(sagacompensation.Table, sagacompensation.Columns, sqlgraph.NewFieldSpec(sagacompensation.FieldID, field.TypeString))
	_spec.From = scq.sql
	if unique := scq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if scq.path != nil {
		_spec.Unique = true
	}
	if fields := scq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, sagacompensation.FieldID)
		for i := range fields {
			if fields[i] != sagacompensation.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := scq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := scq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := scq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := scq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (scq *SagaCompensationQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(scq.driver.Dialect())
	t1 := builder.Table(sagacompensation.Table)
	columns := scq.ctx.Fields
	if len(columns) == 0 {
		columns = sagacompensation.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if scq.sql != nil {
		selector = scq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if scq.ctx.Unique != nil && *scq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range scq.predicates {
		p(selector)
	}
	for _, p := range scq.order {
		p(selector)
	}
	if offset := scq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := scq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// SagaCompensationGroupBy is the group-by builder for SagaCompensation entities.
type SagaCompensationGroupBy struct {
	selector
	build *SagaCompensationQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (scgb *SagaCompensationGroupBy) Aggregate(fns ...AggregateFunc) *SagaCompensationGroupBy {
	scgb.fns = append(scgb.fns, fns...)
	return scgb
}

// Scan applies the selector query and scans the result into the given value.
func (scgb *SagaCompensationGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, scgb.build.ctx, ent.OpQueryGroupBy)
	if err := scgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SagaCompensationQuery, *SagaCompensationGroupBy](ctx, scgb.build, scgb, scgb.build.inters, v)
}

func (scgb *SagaCompensationGroupBy) sqlScan(ctx context.Context, root *SagaCompensationQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(scgb.fns))
	for _, fn := range scgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*scgb.flds)+len(scgb.fns))
		for _, f := range *scgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*scgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := scgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SagaCompensationSelect is the builder for selecting fields of SagaCompensation entities.
type SagaCompensationSelect struct {
	*SagaCompensationQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (scs *SagaCompensationSelect) Aggregate(fns ...AggregateFunc) *SagaCompensationSelect {
	scs.fns = append(scs.fns, fns...)
	return scs
}

// Scan applies the selector query and scans the result into the given value.
func (scs *SagaCompensationSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, scs.ctx, ent.OpQuerySelect)
	if err := scs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SagaCompensationQuery, *SagaCompensationSelect](ctx, scs.SagaCompensationQuery, scs, scs.inters, v)
}

func (scs *SagaCompensationSelect) sqlScan(ctx context.Context, root *SagaCompensationQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(scs.fns))
	for _, fn := range scs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*scs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := scs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
