// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/davidroman0O/tempolite/ent/predicate"
	"github.com/davidroman0O/tempolite/ent/schema"
	"github.com/davidroman0O/tempolite/ent/signaldata"
	"github.com/davidroman0O/tempolite/ent/signalentity"
)

// SignalDataQuery is the builder for querying SignalData entities.
type SignalDataQuery struct {
	config
	ctx        *QueryContext
	order      []signaldata.OrderOption
	inters     []Interceptor
	predicates []predicate.SignalData
	withSignal *SignalEntityQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SignalDataQuery builder.
func (sdq *SignalDataQuery) Where(ps ...predicate.SignalData) *SignalDataQuery {
	sdq.predicates = append(sdq.predicates, ps...)
	return sdq
}

// Limit the number of records to be returned by this query.
func (sdq *SignalDataQuery) Limit(limit int) *SignalDataQuery {
	sdq.ctx.Limit = &limit
	return sdq
}

// Offset to start from.
func (sdq *SignalDataQuery) Offset(offset int) *SignalDataQuery {
	sdq.ctx.Offset = &offset
	return sdq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (sdq *SignalDataQuery) Unique(unique bool) *SignalDataQuery {
	sdq.ctx.Unique = &unique
	return sdq
}

// Order specifies how the records should be ordered.
func (sdq *SignalDataQuery) Order(o ...signaldata.OrderOption) *SignalDataQuery {
	sdq.order = append(sdq.order, o...)
	return sdq
}

// QuerySignal chains the current query on the "signal" edge.
func (sdq *SignalDataQuery) QuerySignal() *SignalEntityQuery {
	query := (&SignalEntityClient{config: sdq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := sdq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := sdq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(signaldata.Table, signaldata.FieldID, selector),
			sqlgraph.To(signalentity.Table, signalentity.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, signaldata.SignalTable, signaldata.SignalColumn),
		)
		fromU = sqlgraph.SetNeighbors(sdq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first SignalData entity from the query.
// Returns a *NotFoundError when no SignalData was found.
func (sdq *SignalDataQuery) First(ctx context.Context) (*SignalData, error) {
	nodes, err := sdq.Limit(1).All(setContextOp(ctx, sdq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{signaldata.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (sdq *SignalDataQuery) FirstX(ctx context.Context) *SignalData {
	node, err := sdq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SignalData ID from the query.
// Returns a *NotFoundError when no SignalData ID was found.
func (sdq *SignalDataQuery) FirstID(ctx context.Context) (id schema.SignalDataID, err error) {
	var ids []schema.SignalDataID
	if ids, err = sdq.Limit(1).IDs(setContextOp(ctx, sdq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{signaldata.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (sdq *SignalDataQuery) FirstIDX(ctx context.Context) schema.SignalDataID {
	id, err := sdq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SignalData entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SignalData entity is found.
// Returns a *NotFoundError when no SignalData entities are found.
func (sdq *SignalDataQuery) Only(ctx context.Context) (*SignalData, error) {
	nodes, err := sdq.Limit(2).All(setContextOp(ctx, sdq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{signaldata.Label}
	default:
		return nil, &NotSingularError{signaldata.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (sdq *SignalDataQuery) OnlyX(ctx context.Context) *SignalData {
	node, err := sdq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SignalData ID in the query.
// Returns a *NotSingularError when more than one SignalData ID is found.
// Returns a *NotFoundError when no entities are found.
func (sdq *SignalDataQuery) OnlyID(ctx context.Context) (id schema.SignalDataID, err error) {
	var ids []schema.SignalDataID
	if ids, err = sdq.Limit(2).IDs(setContextOp(ctx, sdq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{signaldata.Label}
	default:
		err = &NotSingularError{signaldata.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (sdq *SignalDataQuery) OnlyIDX(ctx context.Context) schema.SignalDataID {
	id, err := sdq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SignalDataSlice.
func (sdq *SignalDataQuery) All(ctx context.Context) ([]*SignalData, error) {
	ctx = setContextOp(ctx, sdq.ctx, ent.OpQueryAll)
	if err := sdq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SignalData, *SignalDataQuery]()
	return withInterceptors[[]*SignalData](ctx, sdq, qr, sdq.inters)
}

// AllX is like All, but panics if an error occurs.
func (sdq *SignalDataQuery) AllX(ctx context.Context) []*SignalData {
	nodes, err := sdq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SignalData IDs.
func (sdq *SignalDataQuery) IDs(ctx context.Context) (ids []schema.SignalDataID, err error) {
	if sdq.ctx.Unique == nil && sdq.path != nil {
		sdq.Unique(true)
	}
	ctx = setContextOp(ctx, sdq.ctx, ent.OpQueryIDs)
	if err = sdq.Select(signaldata.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (sdq *SignalDataQuery) IDsX(ctx context.Context) []schema.SignalDataID {
	ids, err := sdq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (sdq *SignalDataQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, sdq.ctx, ent.OpQueryCount)
	if err := sdq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, sdq, querierCount[*SignalDataQuery](), sdq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (sdq *SignalDataQuery) CountX(ctx context.Context) int {
	count, err := sdq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (sdq *SignalDataQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, sdq.ctx, ent.OpQueryExist)
	switch _, err := sdq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (sdq *SignalDataQuery) ExistX(ctx context.Context) bool {
	exist, err := sdq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SignalDataQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (sdq *SignalDataQuery) Clone() *SignalDataQuery {
	if sdq == nil {
		return nil
	}
	return &SignalDataQuery{
		config:     sdq.config,
		ctx:        sdq.ctx.Clone(),
		order:      append([]signaldata.OrderOption{}, sdq.order...),
		inters:     append([]Interceptor{}, sdq.inters...),
		predicates: append([]predicate.SignalData{}, sdq.predicates...),
		withSignal: sdq.withSignal.Clone(),
		// clone intermediate query.
		sql:  sdq.sql.Clone(),
		path: sdq.path,
	}
}

// WithSignal tells the query-builder to eager-load the nodes that are connected to
// the "signal" edge. The optional arguments are used to configure the query builder of the edge.
func (sdq *SignalDataQuery) WithSignal(opts ...func(*SignalEntityQuery)) *SignalDataQuery {
	query := (&SignalEntityClient{config: sdq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	sdq.withSignal = query
	return sdq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		EntityID schema.SignalEntityID `json:"entity_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SignalData.Query().
//		GroupBy(signaldata.FieldEntityID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (sdq *SignalDataQuery) GroupBy(field string, fields ...string) *SignalDataGroupBy {
	sdq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SignalDataGroupBy{build: sdq}
	grbuild.flds = &sdq.ctx.Fields
	grbuild.label = signaldata.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		EntityID schema.SignalEntityID `json:"entity_id,omitempty"`
//	}
//
//	client.SignalData.Query().
//		Select(signaldata.FieldEntityID).
//		Scan(ctx, &v)
func (sdq *SignalDataQuery) Select(fields ...string) *SignalDataSelect {
	sdq.ctx.Fields = append(sdq.ctx.Fields, fields...)
	sbuild := &SignalDataSelect{SignalDataQuery: sdq}
	sbuild.label = signaldata.Label
	sbuild.flds, sbuild.scan = &sdq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SignalDataSelect configured with the given aggregations.
func (sdq *SignalDataQuery) Aggregate(fns ...AggregateFunc) *SignalDataSelect {
	return sdq.Select().Aggregate(fns...)
}

func (sdq *SignalDataQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range sdq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, sdq); err != nil {
				return err
			}
		}
	}
	for _, f := range sdq.ctx.Fields {
		if !signaldata.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if sdq.path != nil {
		prev, err := sdq.path(ctx)
		if err != nil {
			return err
		}
		sdq.sql = prev
	}
	return nil
}

func (sdq *SignalDataQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SignalData, error) {
	var (
		nodes       = []*SignalData{}
		_spec       = sdq.querySpec()
		loadedTypes = [1]bool{
			sdq.withSignal != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SignalData).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SignalData{config: sdq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, sdq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := sdq.withSignal; query != nil {
		if err := sdq.loadSignal(ctx, query, nodes, nil,
			func(n *SignalData, e *SignalEntity) { n.Edges.Signal = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (sdq *SignalDataQuery) loadSignal(ctx context.Context, query *SignalEntityQuery, nodes []*SignalData, init func(*SignalData), assign func(*SignalData, *SignalEntity)) error {
	ids := make([]schema.SignalEntityID, 0, len(nodes))
	nodeids := make(map[schema.SignalEntityID][]*SignalData)
	for i := range nodes {
		fk := nodes[i].EntityID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(signalentity.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "entity_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (sdq *SignalDataQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := sdq.querySpec()
	_spec.Node.Columns = sdq.ctx.Fields
	if len(sdq.ctx.Fields) > 0 {
		_spec.Unique = sdq.ctx.Unique != nil && *sdq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, sdq.driver, _spec)
}

func (sdq *SignalDataQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(signaldata.Table, signaldata.Columns, sqlgraph.NewFieldSpec(signaldata.FieldID, field.TypeInt))
	_spec.From = sdq.sql
	if unique := sdq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if sdq.path != nil {
		_spec.Unique = true
	}
	if fields := sdq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, signaldata.FieldID)
		for i := range fields {
			if fields[i] != signaldata.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if sdq.withSignal != nil {
			_spec.Node.AddColumnOnce(signaldata.FieldEntityID)
		}
	}
	if ps := sdq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := sdq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := sdq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := sdq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (sdq *SignalDataQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(sdq.driver.Dialect())
	t1 := builder.Table(signaldata.Table)
	columns := sdq.ctx.Fields
	if len(columns) == 0 {
		columns = signaldata.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if sdq.sql != nil {
		selector = sdq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if sdq.ctx.Unique != nil && *sdq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range sdq.predicates {
		p(selector)
	}
	for _, p := range sdq.order {
		p(selector)
	}
	if offset := sdq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := sdq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// SignalDataGroupBy is the group-by builder for SignalData entities.
type SignalDataGroupBy struct {
	selector
	build *SignalDataQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (sdgb *SignalDataGroupBy) Aggregate(fns ...AggregateFunc) *SignalDataGroupBy {
	sdgb.fns = append(sdgb.fns, fns...)
	return sdgb
}

// Scan applies the selector query and scans the result into the given value.
func (sdgb *SignalDataGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sdgb.build.ctx, ent.OpQueryGroupBy)
	if err := sdgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SignalDataQuery, *SignalDataGroupBy](ctx, sdgb.build, sdgb, sdgb.build.inters, v)
}

func (sdgb *SignalDataGroupBy) sqlScan(ctx context.Context, root *SignalDataQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(sdgb.fns))
	for _, fn := range sdgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*sdgb.flds)+len(sdgb.fns))
		for _, f := range *sdgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*sdgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sdgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SignalDataSelect is the builder for selecting fields of SignalData entities.
type SignalDataSelect struct {
	*SignalDataQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (sds *SignalDataSelect) Aggregate(fns ...AggregateFunc) *SignalDataSelect {
	sds.fns = append(sds.fns, fns...)
	return sds
}

// Scan applies the selector query and scans the result into the given value.
func (sds *SignalDataSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sds.ctx, ent.OpQuerySelect)
	if err := sds.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SignalDataQuery, *SignalDataSelect](ctx, sds.SignalDataQuery, sds, sds.inters, v)
}

func (sds *SignalDataSelect) sqlScan(ctx context.Context, root *SignalDataQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(sds.fns))
	for _, fn := range sds.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*sds.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sds.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
