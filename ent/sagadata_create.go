// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"time"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/davidroman0O/tempolite/ent/sagadata"
	"github.com/davidroman0O/tempolite/ent/sagaentity"
	"github.com/davidroman0O/tempolite/ent/sagavalue"
	"github.com/davidroman0O/tempolite/ent/schema"
)

// SagaDataCreate is the builder for creating a SagaData entity.
type SagaDataCreate struct {
	config
	mutation *SagaDataMutation
	hooks    []Hook
}

// SetEntityID sets the "entity_id" field.
func (sdc *SagaDataCreate) SetEntityID(sei schema.SagaEntityID) *SagaDataCreate {
	sdc.mutation.SetEntityID(sei)
	return sdc
}

// SetCreatedAt sets the "created_at" field.
func (sdc *SagaDataCreate) SetCreatedAt(t time.Time) *SagaDataCreate {
	sdc.mutation.SetCreatedAt(t)
	return sdc
}

// SetNillableCreatedAt sets the "created_at" field if the given value is not nil.
func (sdc *SagaDataCreate) SetNillableCreatedAt(t *time.Time) *SagaDataCreate {
	if t != nil {
		sdc.SetCreatedAt(*t)
	}
	return sdc
}

// SetUpdatedAt sets the "updated_at" field.
func (sdc *SagaDataCreate) SetUpdatedAt(t time.Time) *SagaDataCreate {
	sdc.mutation.SetUpdatedAt(t)
	return sdc
}

// SetNillableUpdatedAt sets the "updated_at" field if the given value is not nil.
func (sdc *SagaDataCreate) SetNillableUpdatedAt(t *time.Time) *SagaDataCreate {
	if t != nil {
		sdc.SetUpdatedAt(*t)
	}
	return sdc
}

// SetID sets the "id" field.
func (sdc *SagaDataCreate) SetID(sdi schema.SagaDataID) *SagaDataCreate {
	sdc.mutation.SetID(sdi)
	return sdc
}

// SetSagaID sets the "saga" edge to the SagaEntity entity by ID.
func (sdc *SagaDataCreate) SetSagaID(id schema.SagaEntityID) *SagaDataCreate {
	sdc.mutation.SetSagaID(id)
	return sdc
}

// SetSaga sets the "saga" edge to the SagaEntity entity.
func (sdc *SagaDataCreate) SetSaga(s *SagaEntity) *SagaDataCreate {
	return sdc.SetSagaID(s.ID)
}

// AddValueIDs adds the "values" edge to the SagaValue entity by IDs.
func (sdc *SagaDataCreate) AddValueIDs(ids ...schema.SagaValueID) *SagaDataCreate {
	sdc.mutation.AddValueIDs(ids...)
	return sdc
}

// AddValues adds the "values" edges to the SagaValue entity.
func (sdc *SagaDataCreate) AddValues(s ...*SagaValue) *SagaDataCreate {
	ids := make([]schema.SagaValueID, len(s))
	for i := range s {
		ids[i] = s[i].ID
	}
	return sdc.AddValueIDs(ids...)
}

// Mutation returns the SagaDataMutation object of the builder.
func (sdc *SagaDataCreate) Mutation() *SagaDataMutation {
	return sdc.mutation
}

// Save creates the SagaData in the database.
func (sdc *SagaDataCreate) Save(ctx context.Context) (*SagaData, error) {
	sdc.defaults()
	return withHooks(ctx, sdc.sqlSave, sdc.mutation, sdc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (sdc *SagaDataCreate) SaveX(ctx context.Context) *SagaData {
	v, err := sdc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (sdc *SagaDataCreate) Exec(ctx context.Context) error {
	_, err := sdc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (sdc *SagaDataCreate) ExecX(ctx context.Context) {
	if err := sdc.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (sdc *SagaDataCreate) defaults() {
	if _, ok := sdc.mutation.CreatedAt(); !ok {
		v := sagadata.DefaultCreatedAt()
		sdc.mutation.SetCreatedAt(v)
	}
	if _, ok := sdc.mutation.UpdatedAt(); !ok {
		v := sagadata.DefaultUpdatedAt()
		sdc.mutation.SetUpdatedAt(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (sdc *SagaDataCreate) check() error {
	if _, ok := sdc.mutation.EntityID(); !ok {
		return &ValidationError{Name: "entity_id", err: errors.New(`ent: missing required field "SagaData.entity_id"`)}
	}
	if _, ok := sdc.mutation.CreatedAt(); !ok {
		return &ValidationError{Name: "created_at", err: errors.New(`ent: missing required field "SagaData.created_at"`)}
	}
	if _, ok := sdc.mutation.UpdatedAt(); !ok {
		return &ValidationError{Name: "updated_at", err: errors.New(`ent: missing required field "SagaData.updated_at"`)}
	}
	if len(sdc.mutation.SagaIDs()) == 0 {
		return &ValidationError{Name: "saga", err: errors.New(`ent: missing required edge "SagaData.saga"`)}
	}
	return nil
}

func (sdc *SagaDataCreate) sqlSave(ctx context.Context) (*SagaData, error) {
	if err := sdc.check(); err != nil {
		return nil, err
	}
	_node, _spec := sdc.createSpec()
	if err := sqlgraph.CreateNode(ctx, sdc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	if _spec.ID.Value != _node.ID {
		id := _spec.ID.Value.(int64)
		_node.ID = schema.SagaDataID(id)
	}
	sdc.mutation.id = &_node.ID
	sdc.mutation.done = true
	return _node, nil
}

func (sdc *SagaDataCreate) createSpec() (*SagaData, *sqlgraph.CreateSpec) {
	var (
		_node = &SagaData{config: sdc.config}
		_spec = sqlgraph.NewCreateSpec(sagadata.Table, sqlgraph.NewFieldSpec(sagadata.FieldID, field.TypeInt))
	)
	if id, ok := sdc.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = id
	}
	if value, ok := sdc.mutation.CreatedAt(); ok {
		_spec.SetField(sagadata.FieldCreatedAt, field.TypeTime, value)
		_node.CreatedAt = value
	}
	if value, ok := sdc.mutation.UpdatedAt(); ok {
		_spec.SetField(sagadata.FieldUpdatedAt, field.TypeTime, value)
		_node.UpdatedAt = value
	}
	if nodes := sdc.mutation.SagaIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2O,
			Inverse: true,
			Table:   sagadata.SagaTable,
			Columns: []string{sagadata.SagaColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(sagaentity.FieldID, field.TypeInt),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.EntityID = nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	if nodes := sdc.mutation.ValuesIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   sagadata.ValuesTable,
			Columns: []string{sagadata.ValuesColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(sagavalue.FieldID, field.TypeInt),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// SagaDataCreateBulk is the builder for creating many SagaData entities in bulk.
type SagaDataCreateBulk struct {
	config
	err      error
	builders []*SagaDataCreate
}

// Save creates the SagaData entities in the database.
func (sdcb *SagaDataCreateBulk) Save(ctx context.Context) ([]*SagaData, error) {
	if sdcb.err != nil {
		return nil, sdcb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(sdcb.builders))
	nodes := make([]*SagaData, len(sdcb.builders))
	mutators := make([]Mutator, len(sdcb.builders))
	for i := range sdcb.builders {
		func(i int, root context.Context) {
			builder := sdcb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*SagaDataMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, sdcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, sdcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil && nodes[i].ID == 0 {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = schema.SagaDataID(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, sdcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (sdcb *SagaDataCreateBulk) SaveX(ctx context.Context) []*SagaData {
	v, err := sdcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (sdcb *SagaDataCreateBulk) Exec(ctx context.Context) error {
	_, err := sdcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (sdcb *SagaDataCreateBulk) ExecX(ctx context.Context) {
	if err := sdcb.Exec(ctx); err != nil {
		panic(err)
	}
}
