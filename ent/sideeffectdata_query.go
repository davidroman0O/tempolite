// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/davidroman0O/tempolite/ent/predicate"
	"github.com/davidroman0O/tempolite/ent/schema"
	"github.com/davidroman0O/tempolite/ent/sideeffectdata"
	"github.com/davidroman0O/tempolite/ent/sideeffectentity"
)

// SideEffectDataQuery is the builder for querying SideEffectData entities.
type SideEffectDataQuery struct {
	config
	ctx            *QueryContext
	order          []sideeffectdata.OrderOption
	inters         []Interceptor
	predicates     []predicate.SideEffectData
	withSideEffect *SideEffectEntityQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SideEffectDataQuery builder.
func (sedq *SideEffectDataQuery) Where(ps ...predicate.SideEffectData) *SideEffectDataQuery {
	sedq.predicates = append(sedq.predicates, ps...)
	return sedq
}

// Limit the number of records to be returned by this query.
func (sedq *SideEffectDataQuery) Limit(limit int) *SideEffectDataQuery {
	sedq.ctx.Limit = &limit
	return sedq
}

// Offset to start from.
func (sedq *SideEffectDataQuery) Offset(offset int) *SideEffectDataQuery {
	sedq.ctx.Offset = &offset
	return sedq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (sedq *SideEffectDataQuery) Unique(unique bool) *SideEffectDataQuery {
	sedq.ctx.Unique = &unique
	return sedq
}

// Order specifies how the records should be ordered.
func (sedq *SideEffectDataQuery) Order(o ...sideeffectdata.OrderOption) *SideEffectDataQuery {
	sedq.order = append(sedq.order, o...)
	return sedq
}

// QuerySideEffect chains the current query on the "side_effect" edge.
func (sedq *SideEffectDataQuery) QuerySideEffect() *SideEffectEntityQuery {
	query := (&SideEffectEntityClient{config: sedq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := sedq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := sedq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(sideeffectdata.Table, sideeffectdata.FieldID, selector),
			sqlgraph.To(sideeffectentity.Table, sideeffectentity.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, sideeffectdata.SideEffectTable, sideeffectdata.SideEffectColumn),
		)
		fromU = sqlgraph.SetNeighbors(sedq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first SideEffectData entity from the query.
// Returns a *NotFoundError when no SideEffectData was found.
func (sedq *SideEffectDataQuery) First(ctx context.Context) (*SideEffectData, error) {
	nodes, err := sedq.Limit(1).All(setContextOp(ctx, sedq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{sideeffectdata.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (sedq *SideEffectDataQuery) FirstX(ctx context.Context) *SideEffectData {
	node, err := sedq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SideEffectData ID from the query.
// Returns a *NotFoundError when no SideEffectData ID was found.
func (sedq *SideEffectDataQuery) FirstID(ctx context.Context) (id schema.SideEffectDataID, err error) {
	var ids []schema.SideEffectDataID
	if ids, err = sedq.Limit(1).IDs(setContextOp(ctx, sedq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{sideeffectdata.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (sedq *SideEffectDataQuery) FirstIDX(ctx context.Context) schema.SideEffectDataID {
	id, err := sedq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SideEffectData entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SideEffectData entity is found.
// Returns a *NotFoundError when no SideEffectData entities are found.
func (sedq *SideEffectDataQuery) Only(ctx context.Context) (*SideEffectData, error) {
	nodes, err := sedq.Limit(2).All(setContextOp(ctx, sedq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{sideeffectdata.Label}
	default:
		return nil, &NotSingularError{sideeffectdata.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (sedq *SideEffectDataQuery) OnlyX(ctx context.Context) *SideEffectData {
	node, err := sedq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SideEffectData ID in the query.
// Returns a *NotSingularError when more than one SideEffectData ID is found.
// Returns a *NotFoundError when no entities are found.
func (sedq *SideEffectDataQuery) OnlyID(ctx context.Context) (id schema.SideEffectDataID, err error) {
	var ids []schema.SideEffectDataID
	if ids, err = sedq.Limit(2).IDs(setContextOp(ctx, sedq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{sideeffectdata.Label}
	default:
		err = &NotSingularError{sideeffectdata.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (sedq *SideEffectDataQuery) OnlyIDX(ctx context.Context) schema.SideEffectDataID {
	id, err := sedq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SideEffectDataSlice.
func (sedq *SideEffectDataQuery) All(ctx context.Context) ([]*SideEffectData, error) {
	ctx = setContextOp(ctx, sedq.ctx, ent.OpQueryAll)
	if err := sedq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SideEffectData, *SideEffectDataQuery]()
	return withInterceptors[[]*SideEffectData](ctx, sedq, qr, sedq.inters)
}

// AllX is like All, but panics if an error occurs.
func (sedq *SideEffectDataQuery) AllX(ctx context.Context) []*SideEffectData {
	nodes, err := sedq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SideEffectData IDs.
func (sedq *SideEffectDataQuery) IDs(ctx context.Context) (ids []schema.SideEffectDataID, err error) {
	if sedq.ctx.Unique == nil && sedq.path != nil {
		sedq.Unique(true)
	}
	ctx = setContextOp(ctx, sedq.ctx, ent.OpQueryIDs)
	if err = sedq.Select(sideeffectdata.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (sedq *SideEffectDataQuery) IDsX(ctx context.Context) []schema.SideEffectDataID {
	ids, err := sedq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (sedq *SideEffectDataQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, sedq.ctx, ent.OpQueryCount)
	if err := sedq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, sedq, querierCount[*SideEffectDataQuery](), sedq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (sedq *SideEffectDataQuery) CountX(ctx context.Context) int {
	count, err := sedq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (sedq *SideEffectDataQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, sedq.ctx, ent.OpQueryExist)
	switch _, err := sedq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (sedq *SideEffectDataQuery) ExistX(ctx context.Context) bool {
	exist, err := sedq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SideEffectDataQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (sedq *SideEffectDataQuery) Clone() *SideEffectDataQuery {
	if sedq == nil {
		return nil
	}
	return &SideEffectDataQuery{
		config:         sedq.config,
		ctx:            sedq.ctx.Clone(),
		order:          append([]sideeffectdata.OrderOption{}, sedq.order...),
		inters:         append([]Interceptor{}, sedq.inters...),
		predicates:     append([]predicate.SideEffectData{}, sedq.predicates...),
		withSideEffect: sedq.withSideEffect.Clone(),
		// clone intermediate query.
		sql:  sedq.sql.Clone(),
		path: sedq.path,
	}
}

// WithSideEffect tells the query-builder to eager-load the nodes that are connected to
// the "side_effect" edge. The optional arguments are used to configure the query builder of the edge.
func (sedq *SideEffectDataQuery) WithSideEffect(opts ...func(*SideEffectEntityQuery)) *SideEffectDataQuery {
	query := (&SideEffectEntityClient{config: sedq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	sedq.withSideEffect = query
	return sedq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		EntityID schema.SideEffectEntityID `json:"entity_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SideEffectData.Query().
//		GroupBy(sideeffectdata.FieldEntityID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (sedq *SideEffectDataQuery) GroupBy(field string, fields ...string) *SideEffectDataGroupBy {
	sedq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SideEffectDataGroupBy{build: sedq}
	grbuild.flds = &sedq.ctx.Fields
	grbuild.label = sideeffectdata.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		EntityID schema.SideEffectEntityID `json:"entity_id,omitempty"`
//	}
//
//	client.SideEffectData.Query().
//		Select(sideeffectdata.FieldEntityID).
//		Scan(ctx, &v)
func (sedq *SideEffectDataQuery) Select(fields ...string) *SideEffectDataSelect {
	sedq.ctx.Fields = append(sedq.ctx.Fields, fields...)
	sbuild := &SideEffectDataSelect{SideEffectDataQuery: sedq}
	sbuild.label = sideeffectdata.Label
	sbuild.flds, sbuild.scan = &sedq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SideEffectDataSelect configured with the given aggregations.
func (sedq *SideEffectDataQuery) Aggregate(fns ...AggregateFunc) *SideEffectDataSelect {
	return sedq.Select().Aggregate(fns...)
}

func (sedq *SideEffectDataQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range sedq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, sedq); err != nil {
				return err
			}
		}
	}
	for _, f := range sedq.ctx.Fields {
		if !sideeffectdata.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if sedq.path != nil {
		prev, err := sedq.path(ctx)
		if err != nil {
			return err
		}
		sedq.sql = prev
	}
	return nil
}

func (sedq *SideEffectDataQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SideEffectData, error) {
	var (
		nodes       = []*SideEffectData{}
		_spec       = sedq.querySpec()
		loadedTypes = [1]bool{
			sedq.withSideEffect != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SideEffectData).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SideEffectData{config: sedq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, sedq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := sedq.withSideEffect; query != nil {
		if err := sedq.loadSideEffect(ctx, query, nodes, nil,
			func(n *SideEffectData, e *SideEffectEntity) { n.Edges.SideEffect = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (sedq *SideEffectDataQuery) loadSideEffect(ctx context.Context, query *SideEffectEntityQuery, nodes []*SideEffectData, init func(*SideEffectData), assign func(*SideEffectData, *SideEffectEntity)) error {
	ids := make([]schema.SideEffectEntityID, 0, len(nodes))
	nodeids := make(map[schema.SideEffectEntityID][]*SideEffectData)
	for i := range nodes {
		fk := nodes[i].EntityID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(sideeffectentity.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "entity_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (sedq *SideEffectDataQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := sedq.querySpec()
	_spec.Node.Columns = sedq.ctx.Fields
	if len(sedq.ctx.Fields) > 0 {
		_spec.Unique = sedq.ctx.Unique != nil && *sedq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, sedq.driver, _spec)
}

func (sedq *SideEffectDataQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(sideeffectdata.Table, sideeffectdata.Columns, sqlgraph.NewFieldSpec(sideeffectdata.FieldID, field.TypeInt))
	_spec.From = sedq.sql
	if unique := sedq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if sedq.path != nil {
		_spec.Unique = true
	}
	if fields := sedq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, sideeffectdata.FieldID)
		for i := range fields {
			if fields[i] != sideeffectdata.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if sedq.withSideEffect != nil {
			_spec.Node.AddColumnOnce(sideeffectdata.FieldEntityID)
		}
	}
	if ps := sedq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := sedq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := sedq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := sedq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (sedq *SideEffectDataQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(sedq.driver.Dialect())
	t1 := builder.Table(sideeffectdata.Table)
	columns := sedq.ctx.Fields
	if len(columns) == 0 {
		columns = sideeffectdata.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if sedq.sql != nil {
		selector = sedq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if sedq.ctx.Unique != nil && *sedq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range sedq.predicates {
		p(selector)
	}
	for _, p := range sedq.order {
		p(selector)
	}
	if offset := sedq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := sedq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// SideEffectDataGroupBy is the group-by builder for SideEffectData entities.
type SideEffectDataGroupBy struct {
	selector
	build *SideEffectDataQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (sedgb *SideEffectDataGroupBy) Aggregate(fns ...AggregateFunc) *SideEffectDataGroupBy {
	sedgb.fns = append(sedgb.fns, fns...)
	return sedgb
}

// Scan applies the selector query and scans the result into the given value.
func (sedgb *SideEffectDataGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sedgb.build.ctx, ent.OpQueryGroupBy)
	if err := sedgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SideEffectDataQuery, *SideEffectDataGroupBy](ctx, sedgb.build, sedgb, sedgb.build.inters, v)
}

func (sedgb *SideEffectDataGroupBy) sqlScan(ctx context.Context, root *SideEffectDataQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(sedgb.fns))
	for _, fn := range sedgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*sedgb.flds)+len(sedgb.fns))
		for _, f := range *sedgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*sedgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sedgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SideEffectDataSelect is the builder for selecting fields of SideEffectData entities.
type SideEffectDataSelect struct {
	*SideEffectDataQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (seds *SideEffectDataSelect) Aggregate(fns ...AggregateFunc) *SideEffectDataSelect {
	seds.fns = append(seds.fns, fns...)
	return seds
}

// Scan applies the selector query and scans the result into the given value.
func (seds *SideEffectDataSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, seds.ctx, ent.OpQuerySelect)
	if err := seds.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SideEffectDataQuery, *SideEffectDataSelect](ctx, seds.SideEffectDataQuery, seds, seds.inters, v)
}

func (seds *SideEffectDataSelect) sqlScan(ctx context.Context, root *SideEffectDataQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(seds.fns))
	for _, fn := range seds.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*seds.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := seds.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
