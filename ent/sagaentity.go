// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/davidroman0O/tempolite/ent/sagadata"
	"github.com/davidroman0O/tempolite/ent/sagaentity"
	"github.com/davidroman0O/tempolite/ent/schema"
	"github.com/davidroman0O/tempolite/ent/workflowentity"
)

// SagaEntity is the model entity for the SagaEntity schema.
type SagaEntity struct {
	config `json:"-"`
	// ID of the ent.
	ID schema.SagaEntityID `json:"id,omitempty"`
	// HandlerName holds the value of the "handler_name" field.
	HandlerName string `json:"handler_name,omitempty"`
	// Type holds the value of the "type" field.
	Type schema.EntityType `json:"type,omitempty"`
	// Status holds the value of the "status" field.
	Status schema.EntityStatus `json:"status,omitempty"`
	// StepID holds the value of the "step_id" field.
	StepID string `json:"step_id,omitempty"`
	// RunID holds the value of the "run_id" field.
	RunID schema.RunID `json:"run_id,omitempty"`
	// RetryPolicy holds the value of the "retry_policy" field.
	RetryPolicy schema.RetryPolicy `json:"retry_policy,omitempty"`
	// RetryState holds the value of the "retry_state" field.
	RetryState schema.RetryState `json:"retry_state,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SagaEntityQuery when eager-loading is set.
	Edges                         SagaEntityEdges `json:"edges"`
	workflow_entity_saga_children *schema.WorkflowEntityID
	selectValues                  sql.SelectValues
}

// SagaEntityEdges holds the relations/edges for other nodes in the graph.
type SagaEntityEdges struct {
	// Workflow holds the value of the workflow edge.
	Workflow *WorkflowEntity `json:"workflow,omitempty"`
	// SagaData holds the value of the saga_data edge.
	SagaData *SagaData `json:"saga_data,omitempty"`
	// Executions holds the value of the executions edge.
	Executions []*SagaExecution `json:"executions,omitempty"`
	// Values holds the value of the values edge.
	Values []*SagaValue `json:"values,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [4]bool
}

// WorkflowOrErr returns the Workflow value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SagaEntityEdges) WorkflowOrErr() (*WorkflowEntity, error) {
	if e.Workflow != nil {
		return e.Workflow, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: workflowentity.Label}
	}
	return nil, &NotLoadedError{edge: "workflow"}
}

// SagaDataOrErr returns the SagaData value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SagaEntityEdges) SagaDataOrErr() (*SagaData, error) {
	if e.SagaData != nil {
		return e.SagaData, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: sagadata.Label}
	}
	return nil, &NotLoadedError{edge: "saga_data"}
}

// ExecutionsOrErr returns the Executions value or an error if the edge
// was not loaded in eager-loading.
func (e SagaEntityEdges) ExecutionsOrErr() ([]*SagaExecution, error) {
	if e.loadedTypes[2] {
		return e.Executions, nil
	}
	return nil, &NotLoadedError{edge: "executions"}
}

// ValuesOrErr returns the Values value or an error if the edge
// was not loaded in eager-loading.
func (e SagaEntityEdges) ValuesOrErr() ([]*SagaValue, error) {
	if e.loadedTypes[3] {
		return e.Values, nil
	}
	return nil, &NotLoadedError{edge: "values"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SagaEntity) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case sagaentity.FieldRetryPolicy, sagaentity.FieldRetryState:
			values[i] = new([]byte)
		case sagaentity.FieldID, sagaentity.FieldRunID:
			values[i] = new(sql.NullInt64)
		case sagaentity.FieldHandlerName, sagaentity.FieldType, sagaentity.FieldStatus, sagaentity.FieldStepID:
			values[i] = new(sql.NullString)
		case sagaentity.FieldCreatedAt, sagaentity.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case sagaentity.ForeignKeys[0]: // workflow_entity_saga_children
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SagaEntity fields.
func (se *SagaEntity) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case sagaentity.FieldID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				se.ID = schema.SagaEntityID(value.Int64)
			}
		case sagaentity.FieldHandlerName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field handler_name", values[i])
			} else if value.Valid {
				se.HandlerName = value.String
			}
		case sagaentity.FieldType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field type", values[i])
			} else if value.Valid {
				se.Type = schema.EntityType(value.String)
			}
		case sagaentity.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				se.Status = schema.EntityStatus(value.String)
			}
		case sagaentity.FieldStepID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field step_id", values[i])
			} else if value.Valid {
				se.StepID = value.String
			}
		case sagaentity.FieldRunID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field run_id", values[i])
			} else if value.Valid {
				se.RunID = schema.RunID(value.Int64)
			}
		case sagaentity.FieldRetryPolicy:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field retry_policy", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &se.RetryPolicy); err != nil {
					return fmt.Errorf("unmarshal field retry_policy: %w", err)
				}
			}
		case sagaentity.FieldRetryState:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field retry_state", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &se.RetryState); err != nil {
					return fmt.Errorf("unmarshal field retry_state: %w", err)
				}
			}
		case sagaentity.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				se.CreatedAt = value.Time
			}
		case sagaentity.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				se.UpdatedAt = value.Time
			}
		case sagaentity.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field workflow_entity_saga_children", values[i])
			} else if value.Valid {
				se.workflow_entity_saga_children = new(schema.WorkflowEntityID)
				*se.workflow_entity_saga_children = schema.WorkflowEntityID(value.Int64)
			}
		default:
			se.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SagaEntity.
// This includes values selected through modifiers, order, etc.
func (se *SagaEntity) Value(name string) (ent.Value, error) {
	return se.selectValues.Get(name)
}

// QueryWorkflow queries the "workflow" edge of the SagaEntity entity.
func (se *SagaEntity) QueryWorkflow() *WorkflowEntityQuery {
	return NewSagaEntityClient(se.config).QueryWorkflow(se)
}

// QuerySagaData queries the "saga_data" edge of the SagaEntity entity.
func (se *SagaEntity) QuerySagaData() *SagaDataQuery {
	return NewSagaEntityClient(se.config).QuerySagaData(se)
}

// QueryExecutions queries the "executions" edge of the SagaEntity entity.
func (se *SagaEntity) QueryExecutions() *SagaExecutionQuery {
	return NewSagaEntityClient(se.config).QueryExecutions(se)
}

// QueryValues queries the "values" edge of the SagaEntity entity.
func (se *SagaEntity) QueryValues() *SagaValueQuery {
	return NewSagaEntityClient(se.config).QueryValues(se)
}

// Update returns a builder for updating this SagaEntity.
// Note that you need to call SagaEntity.Unwrap() before calling this method if this SagaEntity
// was returned from a transaction, and the transaction was committed or rolled back.
func (se *SagaEntity) Update() *SagaEntityUpdateOne {
	return NewSagaEntityClient(se.config).UpdateOne(se)
}

// Unwrap unwraps the SagaEntity entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (se *SagaEntity) Unwrap() *SagaEntity {
	_tx, ok := se.config.driver.(*txDriver)
	if !ok {
		panic("ent: SagaEntity is not a transactional entity")
	}
	se.config.driver = _tx.drv
	return se
}

// String implements the fmt.Stringer.
func (se *SagaEntity) String() string {
	var builder strings.Builder
	builder.WriteString("SagaEntity(")
	builder.WriteString(fmt.Sprintf("id=%v, ", se.ID))
	builder.WriteString("handler_name=")
	builder.WriteString(se.HandlerName)
	builder.WriteString(", ")
	builder.WriteString("type=")
	builder.WriteString(fmt.Sprintf("%v", se.Type))
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(fmt.Sprintf("%v", se.Status))
	builder.WriteString(", ")
	builder.WriteString("step_id=")
	builder.WriteString(se.StepID)
	builder.WriteString(", ")
	builder.WriteString("run_id=")
	builder.WriteString(fmt.Sprintf("%v", se.RunID))
	builder.WriteString(", ")
	builder.WriteString("retry_policy=")
	builder.WriteString(fmt.Sprintf("%v", se.RetryPolicy))
	builder.WriteString(", ")
	builder.WriteString("retry_state=")
	builder.WriteString(fmt.Sprintf("%v", se.RetryState))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(se.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(se.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// SagaEntities is a parsable slice of SagaEntity.
type SagaEntities []*SagaEntity
