// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/davidroman0O/go-tempolite/ent/executionunit"
	"github.com/davidroman0O/go-tempolite/ent/sagacompensation"
	"github.com/davidroman0O/go-tempolite/ent/sagatransaction"
	"github.com/davidroman0O/go-tempolite/ent/task"
)

// SagaCompensation is the model entity for the SagaCompensation schema.
type SagaCompensation struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// Order holds the value of the "order" field.
	Order int `json:"order,omitempty"`
	// NextCompensationName holds the value of the "next_compensation_name" field.
	NextCompensationName string `json:"next_compensation_name,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SagaCompensationQuery when eager-loading is set.
	Edges                             SagaCompensationEdges `json:"edges"`
	execution_unit_saga_compensations *string
	saga_compensation_task            *string
	saga_transaction_compensation     *string
	selectValues                      sql.SelectValues
}

// SagaCompensationEdges holds the relations/edges for other nodes in the graph.
type SagaCompensationEdges struct {
	// ExecutionUnit holds the value of the execution_unit edge.
	ExecutionUnit *ExecutionUnit `json:"execution_unit,omitempty"`
	// Task holds the value of the task edge.
	Task *Task `json:"task,omitempty"`
	// Transaction holds the value of the transaction edge.
	Transaction *SagaTransaction `json:"transaction,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
}

// ExecutionUnitOrErr returns the ExecutionUnit value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SagaCompensationEdges) ExecutionUnitOrErr() (*ExecutionUnit, error) {
	if e.ExecutionUnit != nil {
		return e.ExecutionUnit, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: executionunit.Label}
	}
	return nil, &NotLoadedError{edge: "execution_unit"}
}

// TaskOrErr returns the Task value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SagaCompensationEdges) TaskOrErr() (*Task, error) {
	if e.Task != nil {
		return e.Task, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: task.Label}
	}
	return nil, &NotLoadedError{edge: "task"}
}

// TransactionOrErr returns the Transaction value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SagaCompensationEdges) TransactionOrErr() (*SagaTransaction, error) {
	if e.Transaction != nil {
		return e.Transaction, nil
	} else if e.loadedTypes[2] {
		return nil, &NotFoundError{label: sagatransaction.Label}
	}
	return nil, &NotLoadedError{edge: "transaction"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SagaCompensation) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case sagacompensation.FieldOrder:
			values[i] = new(sql.NullInt64)
		case sagacompensation.FieldID, sagacompensation.FieldNextCompensationName:
			values[i] = new(sql.NullString)
		case sagacompensation.ForeignKeys[0]: // execution_unit_saga_compensations
			values[i] = new(sql.NullString)
		case sagacompensation.ForeignKeys[1]: // saga_compensation_task
			values[i] = new(sql.NullString)
		case sagacompensation.ForeignKeys[2]: // saga_transaction_compensation
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SagaCompensation fields.
func (sc *SagaCompensation) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case sagacompensation.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				sc.ID = value.String
			}
		case sagacompensation.FieldOrder:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field order", values[i])
			} else if value.Valid {
				sc.Order = int(value.Int64)
			}
		case sagacompensation.FieldNextCompensationName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field next_compensation_name", values[i])
			} else if value.Valid {
				sc.NextCompensationName = value.String
			}
		case sagacompensation.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field execution_unit_saga_compensations", values[i])
			} else if value.Valid {
				sc.execution_unit_saga_compensations = new(string)
				*sc.execution_unit_saga_compensations = value.String
			}
		case sagacompensation.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field saga_compensation_task", values[i])
			} else if value.Valid {
				sc.saga_compensation_task = new(string)
				*sc.saga_compensation_task = value.String
			}
		case sagacompensation.ForeignKeys[2]:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field saga_transaction_compensation", values[i])
			} else if value.Valid {
				sc.saga_transaction_compensation = new(string)
				*sc.saga_transaction_compensation = value.String
			}
		default:
			sc.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SagaCompensation.
// This includes values selected through modifiers, order, etc.
func (sc *SagaCompensation) Value(name string) (ent.Value, error) {
	return sc.selectValues.Get(name)
}

// QueryExecutionUnit queries the "execution_unit" edge of the SagaCompensation entity.
func (sc *SagaCompensation) QueryExecutionUnit() *ExecutionUnitQuery {
	return NewSagaCompensationClient(sc.config).QueryExecutionUnit(sc)
}

// QueryTask queries the "task" edge of the SagaCompensation entity.
func (sc *SagaCompensation) QueryTask() *TaskQuery {
	return NewSagaCompensationClient(sc.config).QueryTask(sc)
}

// QueryTransaction queries the "transaction" edge of the SagaCompensation entity.
func (sc *SagaCompensation) QueryTransaction() *SagaTransactionQuery {
	return NewSagaCompensationClient(sc.config).QueryTransaction(sc)
}

// Update returns a builder for updating this SagaCompensation.
// Note that you need to call SagaCompensation.Unwrap() before calling this method if this SagaCompensation
// was returned from a transaction, and the transaction was committed or rolled back.
func (sc *SagaCompensation) Update() *SagaCompensationUpdateOne {
	return NewSagaCompensationClient(sc.config).UpdateOne(sc)
}

// Unwrap unwraps the SagaCompensation entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (sc *SagaCompensation) Unwrap() *SagaCompensation {
	_tx, ok := sc.config.driver.(*txDriver)
	if !ok {
		panic("ent: SagaCompensation is not a transactional entity")
	}
	sc.config.driver = _tx.drv
	return sc
}

// String implements the fmt.Stringer.
func (sc *SagaCompensation) String() string {
	var builder strings.Builder
	builder.WriteString("SagaCompensation(")
	builder.WriteString(fmt.Sprintf("id=%v, ", sc.ID))
	builder.WriteString("order=")
	builder.WriteString(fmt.Sprintf("%v", sc.Order))
	builder.WriteString(", ")
	builder.WriteString("next_compensation_name=")
	builder.WriteString(sc.NextCompensationName)
	builder.WriteByte(')')
	return builder.String()
}

// SagaCompensations is a parsable slice of SagaCompensation.
type SagaCompensations []*SagaCompensation
