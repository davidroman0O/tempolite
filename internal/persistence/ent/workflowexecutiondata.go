// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/davidroman0O/tempolite/internal/persistence/ent/workflowexecution"
	"github.com/davidroman0O/tempolite/internal/persistence/ent/workflowexecutiondata"
)

// WorkflowExecutionData is the model entity for the WorkflowExecutionData schema.
type WorkflowExecutionData struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// LastHeartbeat holds the value of the "last_heartbeat" field.
	LastHeartbeat *time.Time `json:"last_heartbeat,omitempty"`
	// Outputs holds the value of the "outputs" field.
	Outputs [][]uint8 `json:"outputs,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the WorkflowExecutionDataQuery when eager-loading is set.
	Edges                             WorkflowExecutionDataEdges `json:"edges"`
	workflow_execution_execution_data *int
	selectValues                      sql.SelectValues
}

// WorkflowExecutionDataEdges holds the relations/edges for other nodes in the graph.
type WorkflowExecutionDataEdges struct {
	// WorkflowExecution holds the value of the workflow_execution edge.
	WorkflowExecution *WorkflowExecution `json:"workflow_execution,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// WorkflowExecutionOrErr returns the WorkflowExecution value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e WorkflowExecutionDataEdges) WorkflowExecutionOrErr() (*WorkflowExecution, error) {
	if e.WorkflowExecution != nil {
		return e.WorkflowExecution, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: workflowexecution.Label}
	}
	return nil, &NotLoadedError{edge: "workflow_execution"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*WorkflowExecutionData) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case workflowexecutiondata.FieldOutputs:
			values[i] = new([]byte)
		case workflowexecutiondata.FieldID:
			values[i] = new(sql.NullInt64)
		case workflowexecutiondata.FieldLastHeartbeat:
			values[i] = new(sql.NullTime)
		case workflowexecutiondata.ForeignKeys[0]: // workflow_execution_execution_data
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the WorkflowExecutionData fields.
func (wed *WorkflowExecutionData) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case workflowexecutiondata.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			wed.ID = int(value.Int64)
		case workflowexecutiondata.FieldLastHeartbeat:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_heartbeat", values[i])
			} else if value.Valid {
				wed.LastHeartbeat = new(time.Time)
				*wed.LastHeartbeat = value.Time
			}
		case workflowexecutiondata.FieldOutputs:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field outputs", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &wed.Outputs); err != nil {
					return fmt.Errorf("unmarshal field outputs: %w", err)
				}
			}
		case workflowexecutiondata.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field workflow_execution_execution_data", value)
			} else if value.Valid {
				wed.workflow_execution_execution_data = new(int)
				*wed.workflow_execution_execution_data = int(value.Int64)
			}
		default:
			wed.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the WorkflowExecutionData.
// This includes values selected through modifiers, order, etc.
func (wed *WorkflowExecutionData) Value(name string) (ent.Value, error) {
	return wed.selectValues.Get(name)
}

// QueryWorkflowExecution queries the "workflow_execution" edge of the WorkflowExecutionData entity.
func (wed *WorkflowExecutionData) QueryWorkflowExecution() *WorkflowExecutionQuery {
	return NewWorkflowExecutionDataClient(wed.config).QueryWorkflowExecution(wed)
}

// Update returns a builder for updating this WorkflowExecutionData.
// Note that you need to call WorkflowExecutionData.Unwrap() before calling this method if this WorkflowExecutionData
// was returned from a transaction, and the transaction was committed or rolled back.
func (wed *WorkflowExecutionData) Update() *WorkflowExecutionDataUpdateOne {
	return NewWorkflowExecutionDataClient(wed.config).UpdateOne(wed)
}

// Unwrap unwraps the WorkflowExecutionData entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (wed *WorkflowExecutionData) Unwrap() *WorkflowExecutionData {
	_tx, ok := wed.config.driver.(*txDriver)
	if !ok {
		panic("ent: WorkflowExecutionData is not a transactional entity")
	}
	wed.config.driver = _tx.drv
	return wed
}

// String implements the fmt.Stringer.
func (wed *WorkflowExecutionData) String() string {
	var builder strings.Builder
	builder.WriteString("WorkflowExecutionData(")
	builder.WriteString(fmt.Sprintf("id=%v, ", wed.ID))
	if v := wed.LastHeartbeat; v != nil {
		builder.WriteString("last_heartbeat=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("outputs=")
	builder.WriteString(fmt.Sprintf("%v", wed.Outputs))
	builder.WriteByte(')')
	return builder.String()
}

// WorkflowExecutionDataSlice is a parsable slice of WorkflowExecutionData.
type WorkflowExecutionDataSlice []*WorkflowExecutionData
