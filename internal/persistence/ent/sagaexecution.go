// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/davidroman0O/tempolite/internal/persistence/ent/execution"
	"github.com/davidroman0O/tempolite/internal/persistence/ent/sagaexecution"
	"github.com/davidroman0O/tempolite/internal/persistence/ent/sagaexecutiondata"
)

// SagaExecution is the model entity for the SagaExecution schema.
type SagaExecution struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// StepType holds the value of the "step_type" field.
	StepType sagaexecution.StepType `json:"step_type,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SagaExecutionQuery when eager-loading is set.
	Edges                    SagaExecutionEdges `json:"edges"`
	execution_saga_execution *int
	selectValues             sql.SelectValues
}

// SagaExecutionEdges holds the relations/edges for other nodes in the graph.
type SagaExecutionEdges struct {
	// Execution holds the value of the execution edge.
	Execution *Execution `json:"execution,omitempty"`
	// ExecutionData holds the value of the execution_data edge.
	ExecutionData *SagaExecutionData `json:"execution_data,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// ExecutionOrErr returns the Execution value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SagaExecutionEdges) ExecutionOrErr() (*Execution, error) {
	if e.Execution != nil {
		return e.Execution, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: execution.Label}
	}
	return nil, &NotLoadedError{edge: "execution"}
}

// ExecutionDataOrErr returns the ExecutionData value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SagaExecutionEdges) ExecutionDataOrErr() (*SagaExecutionData, error) {
	if e.ExecutionData != nil {
		return e.ExecutionData, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: sagaexecutiondata.Label}
	}
	return nil, &NotLoadedError{edge: "execution_data"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SagaExecution) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case sagaexecution.FieldID:
			values[i] = new(sql.NullInt64)
		case sagaexecution.FieldStepType:
			values[i] = new(sql.NullString)
		case sagaexecution.ForeignKeys[0]: // execution_saga_execution
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SagaExecution fields.
func (se *SagaExecution) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case sagaexecution.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			se.ID = int(value.Int64)
		case sagaexecution.FieldStepType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field step_type", values[i])
			} else if value.Valid {
				se.StepType = sagaexecution.StepType(value.String)
			}
		case sagaexecution.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field execution_saga_execution", value)
			} else if value.Valid {
				se.execution_saga_execution = new(int)
				*se.execution_saga_execution = int(value.Int64)
			}
		default:
			se.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SagaExecution.
// This includes values selected through modifiers, order, etc.
func (se *SagaExecution) Value(name string) (ent.Value, error) {
	return se.selectValues.Get(name)
}

// QueryExecution queries the "execution" edge of the SagaExecution entity.
func (se *SagaExecution) QueryExecution() *ExecutionQuery {
	return NewSagaExecutionClient(se.config).QueryExecution(se)
}

// QueryExecutionData queries the "execution_data" edge of the SagaExecution entity.
func (se *SagaExecution) QueryExecutionData() *SagaExecutionDataQuery {
	return NewSagaExecutionClient(se.config).QueryExecutionData(se)
}

// Update returns a builder for updating this SagaExecution.
// Note that you need to call SagaExecution.Unwrap() before calling this method if this SagaExecution
// was returned from a transaction, and the transaction was committed or rolled back.
func (se *SagaExecution) Update() *SagaExecutionUpdateOne {
	return NewSagaExecutionClient(se.config).UpdateOne(se)
}

// Unwrap unwraps the SagaExecution entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (se *SagaExecution) Unwrap() *SagaExecution {
	_tx, ok := se.config.driver.(*txDriver)
	if !ok {
		panic("ent: SagaExecution is not a transactional entity")
	}
	se.config.driver = _tx.drv
	return se
}

// String implements the fmt.Stringer.
func (se *SagaExecution) String() string {
	var builder strings.Builder
	builder.WriteString("SagaExecution(")
	builder.WriteString(fmt.Sprintf("id=%v, ", se.ID))
	builder.WriteString("step_type=")
	builder.WriteString(fmt.Sprintf("%v", se.StepType))
	builder.WriteByte(')')
	return builder.String()
}

// SagaExecutions is a parsable slice of SagaExecution.
type SagaExecutions []*SagaExecution
