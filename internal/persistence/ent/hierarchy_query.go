// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/davidroman0O/tempolite/internal/persistence/ent/entity"
	"github.com/davidroman0O/tempolite/internal/persistence/ent/hierarchy"
	"github.com/davidroman0O/tempolite/internal/persistence/ent/predicate"
	"github.com/davidroman0O/tempolite/internal/persistence/ent/run"
)

// HierarchyQuery is the builder for querying Hierarchy entities.
type HierarchyQuery struct {
	config
	ctx              *QueryContext
	order            []hierarchy.OrderOption
	inters           []Interceptor
	predicates       []predicate.Hierarchy
	withRun          *RunQuery
	withParentEntity *EntityQuery
	withChildEntity  *EntityQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the HierarchyQuery builder.
func (hq *HierarchyQuery) Where(ps ...predicate.Hierarchy) *HierarchyQuery {
	hq.predicates = append(hq.predicates, ps...)
	return hq
}

// Limit the number of records to be returned by this query.
func (hq *HierarchyQuery) Limit(limit int) *HierarchyQuery {
	hq.ctx.Limit = &limit
	return hq
}

// Offset to start from.
func (hq *HierarchyQuery) Offset(offset int) *HierarchyQuery {
	hq.ctx.Offset = &offset
	return hq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (hq *HierarchyQuery) Unique(unique bool) *HierarchyQuery {
	hq.ctx.Unique = &unique
	return hq
}

// Order specifies how the records should be ordered.
func (hq *HierarchyQuery) Order(o ...hierarchy.OrderOption) *HierarchyQuery {
	hq.order = append(hq.order, o...)
	return hq
}

// QueryRun chains the current query on the "run" edge.
func (hq *HierarchyQuery) QueryRun() *RunQuery {
	query := (&RunClient{config: hq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := hq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := hq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(hierarchy.Table, hierarchy.FieldID, selector),
			sqlgraph.To(run.Table, run.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, hierarchy.RunTable, hierarchy.RunColumn),
		)
		fromU = sqlgraph.SetNeighbors(hq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryParentEntity chains the current query on the "parent_entity" edge.
func (hq *HierarchyQuery) QueryParentEntity() *EntityQuery {
	query := (&EntityClient{config: hq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := hq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := hq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(hierarchy.Table, hierarchy.FieldID, selector),
			sqlgraph.To(entity.Table, entity.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, hierarchy.ParentEntityTable, hierarchy.ParentEntityColumn),
		)
		fromU = sqlgraph.SetNeighbors(hq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryChildEntity chains the current query on the "child_entity" edge.
func (hq *HierarchyQuery) QueryChildEntity() *EntityQuery {
	query := (&EntityClient{config: hq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := hq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := hq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(hierarchy.Table, hierarchy.FieldID, selector),
			sqlgraph.To(entity.Table, entity.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, hierarchy.ChildEntityTable, hierarchy.ChildEntityColumn),
		)
		fromU = sqlgraph.SetNeighbors(hq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Hierarchy entity from the query.
// Returns a *NotFoundError when no Hierarchy was found.
func (hq *HierarchyQuery) First(ctx context.Context) (*Hierarchy, error) {
	nodes, err := hq.Limit(1).All(setContextOp(ctx, hq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{hierarchy.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (hq *HierarchyQuery) FirstX(ctx context.Context) *Hierarchy {
	node, err := hq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Hierarchy ID from the query.
// Returns a *NotFoundError when no Hierarchy ID was found.
func (hq *HierarchyQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = hq.Limit(1).IDs(setContextOp(ctx, hq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{hierarchy.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (hq *HierarchyQuery) FirstIDX(ctx context.Context) int {
	id, err := hq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Hierarchy entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Hierarchy entity is found.
// Returns a *NotFoundError when no Hierarchy entities are found.
func (hq *HierarchyQuery) Only(ctx context.Context) (*Hierarchy, error) {
	nodes, err := hq.Limit(2).All(setContextOp(ctx, hq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{hierarchy.Label}
	default:
		return nil, &NotSingularError{hierarchy.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (hq *HierarchyQuery) OnlyX(ctx context.Context) *Hierarchy {
	node, err := hq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Hierarchy ID in the query.
// Returns a *NotSingularError when more than one Hierarchy ID is found.
// Returns a *NotFoundError when no entities are found.
func (hq *HierarchyQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = hq.Limit(2).IDs(setContextOp(ctx, hq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{hierarchy.Label}
	default:
		err = &NotSingularError{hierarchy.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (hq *HierarchyQuery) OnlyIDX(ctx context.Context) int {
	id, err := hq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Hierarchies.
func (hq *HierarchyQuery) All(ctx context.Context) ([]*Hierarchy, error) {
	ctx = setContextOp(ctx, hq.ctx, ent.OpQueryAll)
	if err := hq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Hierarchy, *HierarchyQuery]()
	return withInterceptors[[]*Hierarchy](ctx, hq, qr, hq.inters)
}

// AllX is like All, but panics if an error occurs.
func (hq *HierarchyQuery) AllX(ctx context.Context) []*Hierarchy {
	nodes, err := hq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Hierarchy IDs.
func (hq *HierarchyQuery) IDs(ctx context.Context) (ids []int, err error) {
	if hq.ctx.Unique == nil && hq.path != nil {
		hq.Unique(true)
	}
	ctx = setContextOp(ctx, hq.ctx, ent.OpQueryIDs)
	if err = hq.Select(hierarchy.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (hq *HierarchyQuery) IDsX(ctx context.Context) []int {
	ids, err := hq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (hq *HierarchyQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, hq.ctx, ent.OpQueryCount)
	if err := hq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, hq, querierCount[*HierarchyQuery](), hq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (hq *HierarchyQuery) CountX(ctx context.Context) int {
	count, err := hq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (hq *HierarchyQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, hq.ctx, ent.OpQueryExist)
	switch _, err := hq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (hq *HierarchyQuery) ExistX(ctx context.Context) bool {
	exist, err := hq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the HierarchyQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (hq *HierarchyQuery) Clone() *HierarchyQuery {
	if hq == nil {
		return nil
	}
	return &HierarchyQuery{
		config:           hq.config,
		ctx:              hq.ctx.Clone(),
		order:            append([]hierarchy.OrderOption{}, hq.order...),
		inters:           append([]Interceptor{}, hq.inters...),
		predicates:       append([]predicate.Hierarchy{}, hq.predicates...),
		withRun:          hq.withRun.Clone(),
		withParentEntity: hq.withParentEntity.Clone(),
		withChildEntity:  hq.withChildEntity.Clone(),
		// clone intermediate query.
		sql:  hq.sql.Clone(),
		path: hq.path,
	}
}

// WithRun tells the query-builder to eager-load the nodes that are connected to
// the "run" edge. The optional arguments are used to configure the query builder of the edge.
func (hq *HierarchyQuery) WithRun(opts ...func(*RunQuery)) *HierarchyQuery {
	query := (&RunClient{config: hq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	hq.withRun = query
	return hq
}

// WithParentEntity tells the query-builder to eager-load the nodes that are connected to
// the "parent_entity" edge. The optional arguments are used to configure the query builder of the edge.
func (hq *HierarchyQuery) WithParentEntity(opts ...func(*EntityQuery)) *HierarchyQuery {
	query := (&EntityClient{config: hq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	hq.withParentEntity = query
	return hq
}

// WithChildEntity tells the query-builder to eager-load the nodes that are connected to
// the "child_entity" edge. The optional arguments are used to configure the query builder of the edge.
func (hq *HierarchyQuery) WithChildEntity(opts ...func(*EntityQuery)) *HierarchyQuery {
	query := (&EntityClient{config: hq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	hq.withChildEntity = query
	return hq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		RunID int `json:"run_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Hierarchy.Query().
//		GroupBy(hierarchy.FieldRunID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (hq *HierarchyQuery) GroupBy(field string, fields ...string) *HierarchyGroupBy {
	hq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &HierarchyGroupBy{build: hq}
	grbuild.flds = &hq.ctx.Fields
	grbuild.label = hierarchy.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		RunID int `json:"run_id,omitempty"`
//	}
//
//	client.Hierarchy.Query().
//		Select(hierarchy.FieldRunID).
//		Scan(ctx, &v)
func (hq *HierarchyQuery) Select(fields ...string) *HierarchySelect {
	hq.ctx.Fields = append(hq.ctx.Fields, fields...)
	sbuild := &HierarchySelect{HierarchyQuery: hq}
	sbuild.label = hierarchy.Label
	sbuild.flds, sbuild.scan = &hq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a HierarchySelect configured with the given aggregations.
func (hq *HierarchyQuery) Aggregate(fns ...AggregateFunc) *HierarchySelect {
	return hq.Select().Aggregate(fns...)
}

func (hq *HierarchyQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range hq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, hq); err != nil {
				return err
			}
		}
	}
	for _, f := range hq.ctx.Fields {
		if !hierarchy.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if hq.path != nil {
		prev, err := hq.path(ctx)
		if err != nil {
			return err
		}
		hq.sql = prev
	}
	return nil
}

func (hq *HierarchyQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Hierarchy, error) {
	var (
		nodes       = []*Hierarchy{}
		_spec       = hq.querySpec()
		loadedTypes = [3]bool{
			hq.withRun != nil,
			hq.withParentEntity != nil,
			hq.withChildEntity != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Hierarchy).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Hierarchy{config: hq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, hq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := hq.withRun; query != nil {
		if err := hq.loadRun(ctx, query, nodes, nil,
			func(n *Hierarchy, e *Run) { n.Edges.Run = e }); err != nil {
			return nil, err
		}
	}
	if query := hq.withParentEntity; query != nil {
		if err := hq.loadParentEntity(ctx, query, nodes, nil,
			func(n *Hierarchy, e *Entity) { n.Edges.ParentEntity = e }); err != nil {
			return nil, err
		}
	}
	if query := hq.withChildEntity; query != nil {
		if err := hq.loadChildEntity(ctx, query, nodes, nil,
			func(n *Hierarchy, e *Entity) { n.Edges.ChildEntity = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (hq *HierarchyQuery) loadRun(ctx context.Context, query *RunQuery, nodes []*Hierarchy, init func(*Hierarchy), assign func(*Hierarchy, *Run)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Hierarchy)
	for i := range nodes {
		fk := nodes[i].RunID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(run.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "run_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (hq *HierarchyQuery) loadParentEntity(ctx context.Context, query *EntityQuery, nodes []*Hierarchy, init func(*Hierarchy), assign func(*Hierarchy, *Entity)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Hierarchy)
	for i := range nodes {
		fk := nodes[i].ParentEntityID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(entity.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "parent_entity_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (hq *HierarchyQuery) loadChildEntity(ctx context.Context, query *EntityQuery, nodes []*Hierarchy, init func(*Hierarchy), assign func(*Hierarchy, *Entity)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Hierarchy)
	for i := range nodes {
		fk := nodes[i].ChildEntityID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(entity.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "child_entity_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (hq *HierarchyQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := hq.querySpec()
	_spec.Node.Columns = hq.ctx.Fields
	if len(hq.ctx.Fields) > 0 {
		_spec.Unique = hq.ctx.Unique != nil && *hq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, hq.driver, _spec)
}

func (hq *HierarchyQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(hierarchy.Table, hierarchy.Columns, sqlgraph.NewFieldSpec(hierarchy.FieldID, field.TypeInt))
	_spec.From = hq.sql
	if unique := hq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if hq.path != nil {
		_spec.Unique = true
	}
	if fields := hq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, hierarchy.FieldID)
		for i := range fields {
			if fields[i] != hierarchy.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if hq.withRun != nil {
			_spec.Node.AddColumnOnce(hierarchy.FieldRunID)
		}
		if hq.withParentEntity != nil {
			_spec.Node.AddColumnOnce(hierarchy.FieldParentEntityID)
		}
		if hq.withChildEntity != nil {
			_spec.Node.AddColumnOnce(hierarchy.FieldChildEntityID)
		}
	}
	if ps := hq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := hq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := hq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := hq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (hq *HierarchyQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(hq.driver.Dialect())
	t1 := builder.Table(hierarchy.Table)
	columns := hq.ctx.Fields
	if len(columns) == 0 {
		columns = hierarchy.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if hq.sql != nil {
		selector = hq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if hq.ctx.Unique != nil && *hq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range hq.predicates {
		p(selector)
	}
	for _, p := range hq.order {
		p(selector)
	}
	if offset := hq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := hq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// HierarchyGroupBy is the group-by builder for Hierarchy entities.
type HierarchyGroupBy struct {
	selector
	build *HierarchyQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (hgb *HierarchyGroupBy) Aggregate(fns ...AggregateFunc) *HierarchyGroupBy {
	hgb.fns = append(hgb.fns, fns...)
	return hgb
}

// Scan applies the selector query and scans the result into the given value.
func (hgb *HierarchyGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, hgb.build.ctx, ent.OpQueryGroupBy)
	if err := hgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*HierarchyQuery, *HierarchyGroupBy](ctx, hgb.build, hgb, hgb.build.inters, v)
}

func (hgb *HierarchyGroupBy) sqlScan(ctx context.Context, root *HierarchyQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(hgb.fns))
	for _, fn := range hgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*hgb.flds)+len(hgb.fns))
		for _, f := range *hgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*hgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := hgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// HierarchySelect is the builder for selecting fields of Hierarchy entities.
type HierarchySelect struct {
	*HierarchyQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (hs *HierarchySelect) Aggregate(fns ...AggregateFunc) *HierarchySelect {
	hs.fns = append(hs.fns, fns...)
	return hs
}

// Scan applies the selector query and scans the result into the given value.
func (hs *HierarchySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, hs.ctx, ent.OpQuerySelect)
	if err := hs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*HierarchyQuery, *HierarchySelect](ctx, hs.HierarchyQuery, hs, hs.inters, v)
}

func (hs *HierarchySelect) sqlScan(ctx context.Context, root *HierarchyQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(hs.fns))
	for _, fn := range hs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*hs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := hs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
